// CANARY — a pasted copy: its own function with the engine's name AND shape. See README.md.
export function stepBall(ball: unknown, scenario: unknown, rng: () => number, dt: number): null {
  void ball; void scenario; void rng; void dt;
  return null;
}
