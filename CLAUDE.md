# KnowItBall — Project Context

> Auto-loaded every session — keep concise. Full history in `SESSION_LOG.md`. Last updated: 15 September 2026.

---

## What This Project Is

Football games platform at **knowitball.co.uk**. Features: drag-and-drop tierlists, community vote tierlists, PL Draft game, star career game (`/star-dev`), multiplayer draft, tic-tac-toe daily, manager mode.

---

## The User's Local Machine — Never Tell Them To `git pull`/`git checkout`

Confirmed by the user directly, after hundreds of attempts: local `git pull`/`git checkout` does not work for them — do not suggest it, and do not build a workflow around them running it (e.g. "pull my branch, then run this script"). When something needs to reach their machine — a script to run locally, a file to test — **paste the full file contents directly in chat, or give them a link to the file on GitHub**, and have them create the file / copy-paste it into place by hand. This applies to every session, not just one-offs.

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Framework | Next.js 14.2.5 (App Router, TypeScript) |
| Styling | Tailwind CSS 3.4 |
| Drag & Drop | @dnd-kit/core + @dnd-kit/sortable |
| Auth | Supabase Auth (Google OAuth) |
| Database | Supabase PostgreSQL |
| Storage | Supabase Storage (bucket: `tierlist-images`, public) |
| Hosting | Vercel (auto-deploy from main) |
| Image export | html2canvas |
| Image processing | Client-side WebP compression (1200px max, 75% quality) |

---

## Deployment & Branding

- Domain: **knowitball.co.uk** (Hostinger DNS → Vercel)
- Supabase URL: `https://cagkgfketucousksgtbk.supabase.co`
- DB size: ~0.37 GB (500 MB free-tier limit; ~120 MB headroom as of June 2026)
- Site name: **Knowitball Tierlists** | Email: **knowitballcontact@gmail.com**

---

## Environment Variables

```
NEXT_PUBLIC_SUPABASE_URL=https://cagkgfketucousksgtbk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...
NEXT_PUBLIC_APP_URL=https://knowitball.co.uk
NEXT_PUBLIC_POSTHOG_KEY=phc_ksQCEbdcPvcMaAs2u3Ejx6YnpaYYvYQLHGWbcALPskR8
NEXT_PUBLIC_POSTHOG_HOST=https://eu.posthog.com
NEXT_PUBLIC_SENTRY_DSN=            # set once a Sentry project exists — see Pending Setup below
SENTRY_ORG=                        # optional, enables source-map upload at build time
SENTRY_PROJECT=                    # optional, enables source-map upload at build time
SENTRY_AUTH_TOKEN=                 # optional, enables source-map upload at build time
```

Both PostHog and Sentry are wired to no-op cleanly when their env vars are unset — missing `NEXT_PUBLIC_POSTHOG_KEY` disables analytics entirely, missing `NEXT_PUBLIC_SENTRY_DSN` disables error reporting entirely, neither throws or blocks the build. `NEXT_PUBLIC_POSTHOG_KEY` is safe to commit/expose client-side by design (same class of value as a GA measurement ID) but kept in env vars here for consistency with everything else in this table.

---

## Key Design Decisions

1. **No mobile drag-and-drop** — Mobile uses tap-to-select, then tap-tier to place (for vote tierlists). Regular tierlists use @dnd-kit which has pointer sensor support.
2. **Client-side image compression** — Reduces upload size before hitting Supabase Storage. WebP format for smaller files.
3. **Anonymous voting** — localStorage UUID enables voting without login, with one-vote-per-image constraint.
4. **Service-role client** — Used server-side for admin operations and cross-user queries (bypasses RLS).
5. **html2canvas for export** — Screenshots the tier rows div directly, no server-side rendering needed.
6. **Slug generation** — Auto-generated from title with random suffix for uniqueness.

---

## What's Implemented (Complete)

- [x] Drag-and-drop tierlist maker with @dnd-kit
- [x] Image upload to Supabase Storage with WebP compression
- [x] Custom tier rows (add/delete/reorder, custom labels + colors)
- [x] Image tools: zoom, crop, label overlay, remove
- [x] Image style options (square, landscape, portrait, circle, no crop)
- [x] Download tierlist as PNG image
- [x] Share on X (Twitter intent)
- [x] Save as New Tierlist from play mode
- [x] Google OAuth authentication
- [x] User profiles (username, anonymous toggle)
- [x] Login streak tracking
- [x] Like and save/bookmark tierlists
- [x] View count tracking
- [x] Homepage with category-grouped tierlists
- [x] Find a Tierlist page with search + category filter
- [x] Vote tierlists (community polling with live results)
- [x] Anonymous voting support
- [x] Vote tierlist likes
- [x] Admin panel (tierlists, categories, vote tierlists, category settings)
- [x] Admin: bulk image upload, import from regular tierlists
- [x] Admin: drag-and-drop image reordering (replaced arrow buttons) for both tierlist types
- [x] Admin: tier editing (labels, colors, add/remove) for both regular and vote tierlists
- [x] Admin: export backup (JSON download of all data)
- [x] Admin: linked vote tierlist picker on regular tierlists
- [x] Admin: additional categories (multi-select) on regular tierlists
- [x] Admin: cover photo crop for both tierlist types
- [x] Custom tiers saved to DB and loaded on play page (requires `tierlist_tiers.sql` migration)
- [x] Persistent global nav bar
- [x] Legal page (Privacy Policy & Terms of Use)
- [x] Site footer
- [x] Save to Profile feature (screenshot saved to user's profile)
- [x] Face detection (face-api.js TinyFaceDetector) for auto-centering player images (both regular and vote tierlists)
- [x] Admin: face detection on/off toggle per tierlist (auto-runs detection on all images when toggled on and saved)
- [x] Admin: vote tierlist batch save (all changes stage locally until Save Changes — covers cover photo, tiers, images, category, face detection, crop, reorder, import)
- [x] Rebranded to "Knowitball Tierlists" (nav, homepage, footer, 404, metadata)
- [x] Contact email: knowitballcontact@gmail.com (legal page)
- [x] Custom domain: knowitball.co.uk (Hostinger registrar → Vercel hosting)

---

## Pending Migrations / Known Issues

> **IMPORTANT**: Check these before making changes that touch these features.

| Migration File | Status | What It Does |
|----------------|--------|-------------|
| `tierlist_tiers.sql` | **RUN** | Adds `tiers` JSONB column to `tierlists` table. Migration was applied April 2026. Custom tiers for regular tierlists now persist to DB. |
| `sofifa_data.sql` | **RUN** | Creates `sofifa_players` table (sofifa_id, fifa_year, name, positions, club, league, overall, potential, age, image_url, attributes JSONB). Unique on (sofifa_id, fifa_year). |
| `draft_club_seasons.sql` | **PENDING** | Adds `get_pl_club_seasons()` SQL function — fast DISTINCT club/season lookup for the PL Draft clubs API. The API works without it (paginated fallback) but is slower. Run in Supabase SQL Editor. |
| `draft_runs_stats.sql` | **PENDING** | Adds extended stats columns to `draft_runs` (top scorer, assists, clean sheets, streaks, all six cup-winner flags) plus a unique `event_key` for dedup. Until it's run, the history API falls back to the legacy insert: history keeps working but cup/trophy achievements can't unlock and replayed seasons aren't deduped in history. Run in Supabase SQL Editor (idempotent). |
| `draft_records_full_fix.sql` | **PENDING (likely cause of Career Records bug)** | Consolidated fix for `draft_records`/`draft_personal_records` CHECK constraints. The older `draft_records_expanded.sql`/`draft_records_mode.sql`/`draft_records_fix_constraints.sql` migrations were apparently never fully run — their CHECK constraints reject `competition = 'career'` and several `record_type` values (`career_assists`, `career_avg_rating`, `most_points`, `biggest_win`, `avg_rating`), so every Career Records insert silently fails (caught and only `console.error`'d server-side, invisible to users). Run `draft_records_full_fix.sql` in Supabase SQL Editor — it's idempotent and safe to run regardless of which older migrations already applied. |
| `american_draft.sql` | **PENDING** | Creates `american_draft_rooms` and `american_draft_participants` for the standalone dev sandbox at `/draft/american`. Includes RLS policies and Realtime publication. Also patches in `linked_room_code` via `ADD COLUMN IF NOT EXISTS` — re-run it if you applied an earlier version, otherwise the sandbox's final pick fails. Not needed for American mode inside real rooms (see below). |
| `draft_american_mode.sql` | **PENDING** | Adds `american_state` JSONB to `draft_rooms`. Required for the **Draft Mode → American** setting on real multiplayer rooms. Without it the host's "Start Game" returns an error telling you to run this migration. |

### Critical Gotchas

1. **Never add columns to the admin page's initial `select()` query that don't exist in the DB yet** — Supabase returns an error (not empty results) when selecting a non-existent column, which causes the entire query to fail silently and show 0 tierlists. The admin page (`app/admin/page.tsx`) only selects: `id, title, category, cover_image_url, created_at, created_by, slug`. Extra data (tiers, images, etc.) is fetched lazily when opening the edit form.
2. **Supabase Storage structure** — All images are in the `tierlist-images` bucket as flat files with UUID filenames (plus subfolders: `cover-crops/`, `profile-saves/`, `vote-covers/`). There is no per-tierlist folder organization. Image URLs are stored in DB tables (`tierlist_images`, `vote_tierlist_images`).
3. **Image uploads** — Images are compressed client-side to WebP (1200px max, 75% quality) before upload. File names are random UUIDs. Uploaded via Supabase Storage JS client.
4. **Admin panel component (`AdminPanel.tsx`)** — ~2650 lines, contains both regular tierlist and vote tierlist management. Uses `@dnd-kit` for image reordering. Both regular tierlists and vote tierlists use a centralized batch-save pattern (`EditState` for regular, `VoteEditState` for vote) — no changes persist to DB until the user clicks Save Changes.
5. **`NEXT_PUBLIC_SUPABASE_URL` must ALWAYS point to the Supabase project URL** (e.g. `https://cagkgfketucousksgtbk.supabase.co`), NEVER the app/Vercel URL. Setting it to the app URL causes `MIDDLEWARE_INVOCATION_FAILED` (500 on every page).
6. **Supabase OAuth redirect URLs** — Must include `https://knowitball.co.uk/**` (production wildcard) and optionally the Vercel preview wildcard. The **Site URL** in Supabase Auth config must also be `https://knowitball.co.uk`.

---

## What Needs Improvement / Future Work

- [ ] **Mobile drag-and-drop UX** — Regular tierlists could use a tap-to-place system like vote tierlists have
- [ ] **Google Ads integration** — Planned for monetization
- [ ] **Combined XI builder** — Future game mode
- [ ] **Blind rankings** — Future game mode
- [ ] **Match predictions** — Future game mode
- [ ] **Player ratings** — Future game mode
- [ ] **Homepage redesign** — As more game modes are added, homepage needs to feature all games
- [ ] **SEO / Open Graph images** — Dynamic OG images for shared tierlists
- [ ] **Image deletion from Storage** — When tierlists are deleted, orphaned images remain in Supabase Storage (admin delete handler does attempt cleanup, but some orphans may remain)
- [ ] **Storage organization** — All images are flat in the bucket root with UUID names; could be organized into per-tierlist folders for clarity (would require migrating existing URLs)
- [ ] **Rate limiting** — No rate limiting on API routes currently
- [x] ~~**Error boundaries**~~ — Done (June 2026): `app/error.tsx` + `app/global-error.tsx`
- [ ] **Loading states** — Some pages could use skeleton loaders
- [ ] **Tierlist editing** — Can only create new, cannot edit existing tierlists after publishing
- [ ] **Sort/filter on homepage** — Users can only browse by category, no sort controls
- [ ] **PWA support** — Could be installable as a mobile app
- [x] ~~**Run `tierlist_tiers.sql` migration**~~ — Done (April 2026)
- [ ] **Online multiplayer draft** — Multiple players draft from the same PL pool (first come first served, no duplicate picks). Needs Supabase Realtime for turn sync. Relegated players get sacked; remaining players continue.
- [ ] **Player trait system (PL Draft)** — Traits that players can have or be assigned: Captain, Wonderkid, Timeless, Big Game Player, Reckless, Selfish. Not yet designed — just an idea to explore.
- [ ] **Star career: random/triggered life events** — a big brainstormed idea bank of football/footballer-life events (match-fixing bribes, doping temptation, scandal, investments, injuries, family, media, wheel-spin/coin-flip/guaranteed mechanics alongside choice-based ones) meant to keep the player always half-expecting something. Not designed against real code yet — full list captured in `STAR_LIFE_EVENTS.md`. Most natural fit is extending `lib/star/dilemmas.ts`'s choice-event shape, plus a new non-decision event shape for the wheel/coin/guaranteed ones.
- [ ] **Star career: more sponsorship types** — asked directly for a brainstorm of every plausible type of sponsorship a footballer picks up, not just the ten categories `lib/star/sponsors.ts` already has (Boots, Sports Drink, Food, Sports Clothing, Casual Clothing, Electronics, Cosmetics, Watch, Jewelry, Car). Full idea bank — new ongoing categories (position-gated, nation-gated, league-gated — three axes the current ten don't touch at all), one-off milestone deals (first goal, international debut, Ballon d'Or), and mechanic ideas beyond the current fee+objective shape (bidding wars, exclusivity, rival poaching, scandal-by-association, renewal roulette) — captured in `STAR_SPONSOR_IDEAS.md`, same brainstorm-idea-bank spirit as `STAR_LIFE_EVENTS.md`. All brand names in it are invented, not real companies.
- [x] ~~**Star career: Power & Politics ("the Rule Book")**~~ — ALL 7 PHASES of the rollout plan done, plus an unplanned Phase 8 (11 Sep 2026). Reputation, a generic voting/ceremony engine, deepened club ownership (recommendations, formations, kit vote, presidency, the §4.5 son mechanic, mergers), the Rule Book (points per result, no-draws-to-penalties, match length, offside toggle, European slot reallocation, forced league movement, new competition creation), corruption (bribery, lawyers, the black market), club facilities (real per-club stadium/training-ground/youth-academy, majority-owner upgrades, stadium revenue), and — Phase 8 — becoming president of a real governing body (`lib/star/leadership.ts`; two real thresholds, a real vote, and it genuinely bypasses the ordinary influence gate on three real powers once won) are all real and tested. Two rule fields (squad size, Champions League format) stay real, votable data by DELIBERATE CHOICE, not oversight — asked directly to wire real hooks for these, and concluded a safe partial hook doesn't exist for squad size (the only safe option, roster/bench size, would misrepresent what the rule means: on-pitch numbers) and the Champions League rewrite is genuinely multi-session-scale (`euro.ts` is 731 lines tightly keyed to a single league-table phase). Both stay open, honestly scoped, concrete next steps. Full brief and the rollout plan (with each phase's real implementation notes and every honest simplification) are in `STAR_POWER_POLITICS.md` — read that file before extending any of this further.
- [x] ~~**Star career: build `energy` as a real New-Star-Soccer-style gameplay mechanic**~~ — Done (31 Aug 2026). Built exactly to the three-point brief: a hard floor on team selection below which you're never started (`MIN_ENERGY_TO_START`) or even benched (`MIN_ENERGY_TO_SUB`, both in `lib/star/selection.ts`), and regen gated behind a deliberate choice — Rest or the new "Skip to Match Day" action (`lib/star/week.ts`) — never an automatic weekly top-up. `CareerState.energy` drains on `creditMatchResult` proportional to minutes played, resets at season rollover, and drives a transient in-match `liveEnergyAt()` in `CanvasMatch.tsx` that feeds a reinstated `tiredSkills()`, `hookCheck`'s reinstated `"legs"` reason, and `hiddenMatch.ts`'s involvement formula — all backward-compatible for callers (the star-match-dev fork) that don't track it. Paired with a genuinely new **injury system** (`CareerState.injury`, rolled per match, risk climbing with fatigue, forces `"Injured"` status, clears on a weekly countdown or season rollover) — nothing like it existed before. See `tests/star/energy.mts` (new), plus `week.mts`/`selection.mts`/`hiddenMatch.mts` extended.

---

## Recent Session Changes

Full session-by-session history moved to `SESSION_LOG.md` (not auto-loaded as context — read it only when you need historical detail). Latest session: 12 July 2026 — full-site pre-launch audit (7 parallel analysis agents) + fixes across all game modes: multiplayer season-2 ready deadlock, ghost players/leave endpoint, stale season replay, XP/history double-crediting (deterministic run keys), wrong CL crediting, lost trophy history (new pending `draft_runs_stats.sql` migration), UCL pot duplicates, custom tiers now persisted on tierlist publish, Find/vote >1000-row truncation, anonymous voter-ID crashes, tic-tac-toe future dailies + score validation, manager-mode soft-locks, and more. Previous session: 30 June 2026 (cont. 4) — added editable multiplayer team names (replacing the hardcoded "{display_name} FC"); added continent-based and exclusion-based objective conditions; redesigned the multiplayer lobby host settings UI; fixed several objective-admin bugs (card removal, tab sizing, toast duration); fixed a real bug where XP/objectives/history weren't credited if a player navigated away during the season-result reveal animation (crediting was wrongly gated behind the animation finishing instead of the already-computed result). See `SESSION_LOG.md` for full detail on this and all prior sessions.

---

## Commands

```bash
npm run dev    # Start development server
npm run build  # Production build
npm run lint   # Run ESLint
```

---

## Pending Migrations

> Run in Supabase SQL Editor before touching related features.

| File | Status | Impact if missing |
|------|--------|-------------------|
| `draft_club_seasons.sql` | **PENDING** | PL Draft clubs API works but is slower (paginated fallback) |
| `draft_runs_stats.sql` | **PENDING** | Cup/trophy achievements can't unlock; replayed seasons not deduped in history |
| `draft_records_full_fix.sql` | **PENDING** | Career Records silently fail to insert (CHECK constraint mismatch on `competition`/`record_type` values) |
| `draft_american_mode.sql` | **PENDING** | American draft mode can't start on real multiplayer rooms (`draft_rooms.american_state` missing) |
| `sofifa_search_indexes.sql` | **PENDING** | Trigram indexes on `sofifa_players.positions`/`league`. The American draft no longer needs them (it filters by league equality and positions in JS), but without them any ILIKE query on those columns scans the whole table and times out. |
| `perf_indexes_jul2026.sql` | **PENDING** | `tierlist_likes` has no index leading with `tierlist_id` despite every /play and /vote view counting on it; `draft_records` has none at all. |
| `security_rls_hardening_jul2026.sql` | **PENDING — HIGHEST PRIORITY** | Closes 6 confirmed RLS holes reachable by anyone with the public anon key: writing your own XP/level and rewards, deleting every objective or every user's progression site-wide, rewriting or wiping every community vote, and writing your own `team_strength`/`status` straight into `draft_room_players` (which the simulate route trusts). Idempotent; verify with its §10 query. |
| `fix_two_digit_fifa_years.sql` | **PENDING** (new) | Clears up rows the admin clone form wrote with a two-digit `fifa_year` (26 instead of 2027) and an age of `23 + (26 - 2026) = -1977`. They render as "FC 26" because the label helper normalises anything over 100, so they hide next to the real row. The form and the API are both fixed; this is the clean-up. Look-first queries, deletes commented out. |
| `fc27_clone_premier_league.sql` | **RUN** (Aug 2026) | Cloned every FC 26 Premier League player into `fifa_year = 2027` a year older, so the 2026/27 season can be built by hand before FC 27 exists. 506 players, 20 clubs, every club's average age +1.0. It is a single statement whose result row says what happened, and it matches on **club name** (the twenty in `lib/star/kits.ts`, punctuation stripped) rather than league name — the league-name version found nothing twice. `ON CONFLICT DO NOTHING`, so re-run it to fill in anybody missing; it never undoes an edit. Manual promotions from other leagues survive re-runs (Arsenal 24→26, Newcastle 29→28 are exactly that). |
| `security_user_profiles_columns_aug2026.sql` | **PENDING** (new, Aug 2026) | The `user_profiles` update policy limits you to your own row but permits any *column*. Lets any logged-in user equip cosmetics they never unlocked (bypassing `/api/profile/equip`), set `longest_streak` to grant themselves streak trophies via `/api/stats`, and bypass the username-change cooldown. Run `security_rls_hardening_jul2026.sql` first. |
| `star_lineups.sql` | **PENDING — RUN BEFORE USING /lineups AGAIN** (new, Aug 2026) | Creates the `star_lineups` table. The Lineups/Squad Builder page used to save ONLY to browser localStorage — invisible to every other device and every other player, discovered after real work was put into building lineups that only that one browser ever saw. `/api/star/lineups` (GET public, POST admin-only via `isAdmin()`) now reads/writes this table instead; `lib/star/lineupStore.ts`'s localStorage stays as a synchronous read cache, refreshed via `fetchSharedLineups()` at app load. Until this migration runs, GET/POST both fail (table doesn't exist) and the Lineups page falls back to auto-picked sides for everyone. |
| `high_potential.sql` | **PENDING — RUN TO ENABLE WONDERKIDS** (new, Sep 2026) | Adds `sofifa_players.high_potential boolean NOT NULL DEFAULT false`. The new toggle on the base row in `/admin/football/players` (always targets the FC 27 edition — see `STAR_FIFA_YEAR`) writes to this column via the existing PATCH route; the Star Career game reads it as `LeaguePlayer.highPotential`/`SquadPlayer.highPotential` to drive `growWonderkids` (season-rollover overall growth), `feeFor`'s transfer-fee premium, the transfer engine's bigger-club reach bonus, and a new weekly media-hype detector (`lib/star/media/detect/wonderkids.ts`). **11 Sep 2026 fix**: `app/api/star/league-squads/route.ts` originally selected this column unconditionally — since Supabase fails the WHOLE query when a column doesn't exist (not just that field), this broke EVERY real squad fetch the moment the column was added to the select, silently falling every club in every team sheet back to `generatedSquad`'s fake roster (reported directly: "both teams all have silhouettes... made up players... for every single club"). The route now retries, dropping one potential-tier column at a time, if the query errors, so a pending migration degrades to "no wonderkid flag yet," not "no real players at all" — but wonderkids themselves still need this migration run to do anything. |
| `world_class_potential.sql` | **PENDING — RUN TO ENABLE THE STRONGER TIER** (new, Sep 2026) | Adds `sofifa_players.world_class_potential boolean NOT NULL DEFAULT false` — a second, stronger tier directly above `high_potential`, same admin-toggle shape, requested directly ("a second category... nothing, high potential, or world class potential"). The admin PATCH route always sets `high_potential = true` alongside it, so every existing High Potential hook (`growWonderkids`, `feeFor`, the reach-up bias, the media hype detector) already fires for a World Class player without its own copy of each check — `wonderkids.worldClassMultiplier` (tuning.ts) scales all of them up further on top. Same resilience as `high_potential`: `league-squads/route.ts` degrades gracefully if this column doesn't exist yet, one column at a time. |
| `star_career_slots.sql` | **PENDING — RUN SO EXTRA SAVES FOLLOW A PLAYER ACROSS DEVICES** (new, Sep 2026) | Adds `star_careers.slot` (default 1, backfilling every existing save automatically) and swaps its `UNIQUE (user_id)` for `UNIQUE (user_id, slot)`, so an account can have up to three cloud saves instead of one — see Settings' new Saves panel. `app/api/star/career/route.ts` degrades the same way `league-squads/route.ts` already does: slot 1 (every existing save) works identically whether or not this has run; a second or third save simply stays local to whichever device created it until it has. |

---

## Pending Setup (non-database, Sep 2026)

| What | Status | Impact if missing |
|------|--------|-------------------|
| Sentry project | **PENDING** | The error-monitoring connector's org link (`knowitball`) is correct, but the org has no project in it yet — Sentry dashboard → Projects → Create Project → "Next.js" platform. `instrumentation-client.ts`/`sentry.server.config.ts`/`sentry.edge.config.ts` are already wired to read `NEXT_PUBLIC_SENTRY_DSN`; until that env var is set in Vercel, error reporting is a deliberate no-op (not broken — just off). |
| Remote connectors (Supabase, Vercel, Adobe, PostHog, Sentry) unusable from a *remote/cloud* Claude Code session | **Not a reconnect/account issue — confirmed Sep 2026** | Every one of them fails identically with `SdkHttpError ... CLIENT_HTTP_NOT_IMPLEMENTED` dialing Anthropic's own MCP bridge, even though `ListConnectors` shows each one properly `connected`/`enabledInChat`. The local network proxy is healthy and irrelevant (`api.anthropic.com` is routed around it). This looks like a limitation of running inside a remote/cloud "Claude Code on the web" container specifically, not of any one connector or account — an earlier note here guessing "wrong Supabase/Vercel account, disconnect and reconnect" was superseded by this and shouldn't be acted on. The likely (not independently verified) fix is running Claude Code as a genuinely NEW session on the user's own machine (desktop app or local CLI) against this same repo/branch, rather than resuming this remote session. |

---

## Critical Gotchas

1. **Admin page `select()` query** — only selects `id, title, category, cover_image_url, created_at, created_by, slug`. Adding a non-existent column causes Supabase to error silently and show 0 tierlists. Extra data is fetched lazily when opening the edit form.
2. **`NEXT_PUBLIC_SUPABASE_URL`** must point to the Supabase project URL (`https://cagkgfketucousksgtbk.supabase.co`), NEVER the app/Vercel URL. Wrong value → `MIDDLEWARE_INVOCATION_FAILED` on every page.
3. **Supabase OAuth redirect URLs** — must include `https://knowitball.co.uk/**`. The Site URL in Supabase Auth config must also be `https://knowitball.co.uk`.
4. **Storage bucket** — flat UUID filenames; no per-tierlist folders. Subfolders: `cover-crops/`, `profile-saves/`, `vote-covers/`.
5. **AdminPanel.tsx** — ~2650 lines, batch-save pattern (`EditState` / `VoteEditState`). Nothing persists to DB until "Save Changes" is clicked.
6. **Anonymous voting** — uses `localStorage` UUID as voter identity (one vote per image per device).
7. **"Use their shortened names" (clubs)** — means `CLUB_SHORT_NAMES` in `lib/star/clubs.ts`, a given (not guessed) mapping for every club on the English ladder (Nottingham Forest → Forest, Tottenham Hotspur → Spurs, Crystal Palace → Palace, Manchester United → Man United, etc. — Man United, not Man Utd). `shortClub()` (`lib/star/media/grammar.ts`) reads it first; only a club outside that list (Champions League/Europa League/Other) falls back to the old suffix-stripping heuristic. Update the one table, not individual call sites, if a short name ever needs to change.

---

## PL Draft — Data Status (August 2026)

- `sofifa_players` table exists, scraped and imported for every PL edition the user needs.
- **SoFIFA's CDN now requires a signed-in session to serve a player face image at all** — every `image_url` in the table is a raw `cdn.sofifa.net` link, so both the live site's `<img>` tags AND the scraper's own anonymous fetches started failing around August 2026. This affects the star career game's current roster (fixed) and Draft mode's whole historical archive (fixed one edition at a time, see below) — anywhere else a `sofifa_players.image_url` is rendered directly is still exposed to this.
- **Fix**: self-host a copy in Supabase Storage (`tierlist-images` bucket, `player-portraits/`) instead of hotlinking SoFIFA.
  - `scripts/scrape_missing.py --year=YYYY --league=13 --download-faces` — downloads faces via a real `<img>` DOM element + Playwright network capture (a plain fetch gets CORS-blocked; this bypasses it), saves to `sofifa_data/faces/{year}/{sofifa_id}.png`.
  - `scripts/upload_player_images.py` — uploads just the current season's ~506-player roster (`fifa_year = STAR_FIFA_YEAR`), used by the star career game's team sheets. Path: `player-portraits/{sofifa_id}.png` (no year suffix — one real-world photo per player, shared across editions on purpose).
  - `scripts/upload_pl_draft_images.py --year=YYYY` — same idea for ONE Premier League edition of Draft mode's archive at a time (FA/League Cup Draft, and Champions/Europa League's PL entrants indirectly via `career.league`). Path: `player-portraits/{sofifa_id}-{year}.png` — year-suffixed here, deliberately, because the SAME real player can have a genuinely different photo across different FIFA editions and sharing one file would let a later edition's upload silently overwrite an earlier one's.
  - Run the scrape+upload pair per edition, in whatever order — no dependency between years. As of August 2026, only the current season and the seasons already re-scraped for faces have real images; everything else falls back to the silhouette (see below) until scraped.
- Import at: `/admin/football/scrape` → `POST /api/admin/football/import-sofifa` (unrelated to the image pipeline above — this is for player DATA, not photos).

---

## Star Career Game (`/star-dev`) — Key Files

- `app/star-dev/page.tsx` — phase state machine + localStorage persistence
- `lib/star/types.ts` — all interfaces (`CareerState`, `SquadPlayer`, `GoalEvent`, etc.)
- `lib/star/careerFlow.ts` — pure reducers: `makeInitialCareer`, `creditMatchResult`, `advanceSeason`
- `lib/star/matchStats.ts` — `finaliseMatch`: converts raw match tally → `MatchStats`
- `lib/star/squadData.ts` — `generateSquad(seed)`: named 20-player squads per club
- `lib/star/season.ts` — league/fixture builders, `simulateOtherFixtures`
- `components/star/CanvasMatch.tsx` — canvas match engine (goal events, commentary)
- `components/star/LeagueScreen.tsx` — Table / Fixtures / Squad tabs
- `lib/star/canvasEngine.ts` — all match physics + AI: ball flight, keeper, defenders,
  support play, space evaluation, interception, aerial duels, chaining, vision
- `lib/star/hiddenMatch.ts` — the ninety minutes you are not playing (possession,
  territory, momentum, scenario requests)
- `lib/star/cups.ts` — FA Cup/League Cup: 32-club hat draw, `playCupRound`,
  `finishCupToWinner` (plays a cup out to a real winner once you're eliminated)
- `lib/star/euro.ts` — Champions/Europa/Conference League: league phase + knockout,
  `crownEurope` (winner when you're eliminated), `crownWithoutYou` (winner in a
  season you were never entered at all — see competitions.ts's `seasonQualifiers`)
- `lib/star/competitions.ts` — `qualificationFor`/`seasonQualifiers` (who earns
  which European spot, applied to the whole division), `seedPreSeason`
  (Community Shield/Super Cup, reads `career.lastSeasonWinners`)
- `lib/star/leagueSquads.ts` — the other 19 clubs' rosters; `averageStartingXIRating`/
  `syncLeagueStrengthFromSquads` derive `LeagueTeam.strength` from the squad's
  actual starting XI rather than a fixed roll — resync after any `leagueSquads` change
- `components/star/CupDrawReveal.tsx` — the "run the draw" reveal screen for
  cup/Europe knockout rounds, wired into `continueAfterMatch` in `page.tsx`
- `lib/silhouette.ts` — `SILHOUETTE_SRC`, the one placeholder image for a player
  with no photo, shared by every screen in both Draft mode and NSS mode
- `/star-match-dev` (admin-only, unlinked) — a FULL FORK of the match engine
  (`canvasEngineTest.ts`/`hiddenMatchTest.ts`/`CanvasMatchTest.tsx`) for trying out
  gameplay physics changes without touching real careers. Changes there do NOT
  reach `/star-dev` until manually ported to the production files.
- `tests/star/*.mts` — 36 suites. **Run them before changing match behaviour**;
  `tests/star/README.md` records the tuned distributions and the mistakes the
  measurements caught. `for f in tests/star/*.mts; do npx tsx "$f"; done`

---

## Recent Session

**15 September 2026 (cont. 5) — The outline reverted to a real alpha-silhouette trace — the landmark-contour attempt excluded hair, and that's not what was wanted.**

- **The ask.** Direct, after the previous entry's 68-point jaw-landmark outline: "the FACES are now outlined, not the whole head lol. like not including the hair... i just want the IMAGE (not the transparent areas) as in the visible image; so the head; face and hair and whatever is visible, to be able to be outlined." A genuinely different requirement than either shape-based attempt so far — not "a shape approximating a face," but literally the photo's own real alpha boundary, whatever it actually is (head, hair, all of it).
- **This settles, for real this time, whether these photos have actual transparency** — the very first attempt at this outline (14 Sep, cont. 5) assumed they did and was later corrected on the belief they were plain opaque rectangles (leading to the crop tool). That correction was about needing to exclude EXTRA opaque content (neck, shirt collar) from the crop — it never actually proved there's no transparency at all. This round's direct instruction is the real, live confirmation: the photos DO have a real alpha channel around a head+hair cutout, and tracing it is exactly what was being asked for from the start.
- **The landmark-contour machinery from the previous entry is gone, not left disabled** — `lib/star/faceOutline.ts`, `tests/star/faceOutline.mts`, `detectFaceLandmarksFromUrl`/`ensureLandmarkModelLoaded`/`FaceLandmarkData` (`lib/faceDetection.ts`), and the downloaded `face_landmark_68_model-*` weight files (`public/models/`) are all deleted outright — a jaw-only contour was never going to include hair no matter how it was tuned, since no facial-landmark model tracks hair at all; this wasn't a tuning problem.
- **The real fix is the ORIGINAL alpha-silhouette technique from 14 September (cont. 5), rebuilt** — this was already built once, then removed on the (now corrected) belief that photos have no transparency, so the technique itself needed no re-invention, just reconciling with the crop tool that didn't exist yet the first time. `drawPlayerHead.ts`: `silhouetteFor()` recolours the photo's own real alpha shape via `globalCompositeOperation = "source-in"` (unchanged from the original), cached per (photo, colour) in a `WeakMap`; `stampOutline()` stamps that silhouette at 12 ring points, all sampling the SAME cropped source rectangle (`style.crop` via `sourceRect`) the real photo itself draws — so the ring "peeks out" exactly where that photo's own real edge, WITHIN the current crop, actually is. The real photo then draws on top, unclipped (no more circle clip — a circle would trim away exactly the hair detail the outline exists to trace); the backing circle (unchanged, still its own toggle) shows through anywhere the photo's own shape doesn't reach.
- **A player with no photo keeps a plain circular stroke** — there's no real alpha shape to trace in that case, nothing dishonest about falling back to a plain circle rather than faking one.
- Full 108-file suite (109 minus the deleted `faceOutline.mts`) and `tsc --noEmit` both clean. Also verified with a real local production build (`npm run build`, exit 0) given this deletes files and drops a model dependency — not just assumed safe.
- As with the alpha-silhouette technique's own first outing, this rests on the same real, standard canvas trick and the same edge case worth a genuine live look: whether these photos' backgrounds are cleanly transparent all the way to the actual hair edge, or have any soft/anti-aliased fringe that could look rough once dilated and stamped. Worth checking against an actual signed-in view before trusting the visual result completely.

**15 September 2026 (cont. 4) — Default offsets corrected, and a real 68-point facial-landmark jaw contour — later found to still be wrong (see the entry above).**

- **Two real corrections shipped this round, then the second one itself corrected one entry later.** First: `DEFAULT_FACE_STYLE`'s offsetX/offsetY corrected to the user's own actual slider values (-0.15 / -1.95), replacing an earlier misreading of the original prose (0.03 / -2.04).
- **Second, larger piece**: reported directly that the box-derived ellipse outline from two entries ago was "still... just doing a circular outline" — root-caused as a real padding-math bug (the padding shape converged toward square/circular regardless of the input box, ~6% taller than wide) as well as a deeper limitation (a bounding box can't describe a non-rectangular contour at all). Fixed by adding face-api.js's 68-point facial landmark model (fetched from the library's own official weights repository) alongside the existing TinyFaceDetector, and tracing `getJawOutline()`'s real 17 points — that specific photo's own detected jaw/cheek/chin curve — capped with a proportional forehead/crown approximation (no landmark model tracks hair/forehead at all, stated as the real limitation it is, not hidden). Verified against face-api.js's own real TypeScript definitions (`tsc` catching any API mismatch) and a real production build.
- **Reported back immediately afterward as still wrong** — a jaw contour structurally excludes hair, which turned out to be exactly what was wanted included. See the entry above for the real fix; this entry stays as an honest record of the attempt, not silently erased.

**15 September 2026 (cont. 3) — The admin global-default database mechanism ripped back out; the outline's fallback is a real oval, not a circle.**

- **"why does it need to be in a database if u already had default values... just change those default values."** Correct — the whole `star_face_style_default` table/migration/API route/"Set as Global Default" button from two entries ago was real, working infrastructure for a problem that didn't need it: `DEFAULT_FACE_STYLE` is already the one hardcoded starting point every device with no local override opens on, so "set the official default" just means changing that constant. Deleted outright, not left disabled: the migration file, `app/api/star/face-style-default/`, `hasFaceStyleOverride`/`fetchGlobalDefaultFaceStyle` and every call site, the "Set as Global Default (Admin)" button, and "Reset to Default"'s global-default target (back to plain `DEFAULT_FACE_STYLE`). `DEFAULT_FACE_STYLE` itself now IS the given values directly: `scale: 3, offsetX: 0.03, offsetY: -2.04, crop.zoom: 1.1` — same reading as before (the repeated "left/right" as "up/down," "10% crop" as a modest zoom), just landing in the one constant instead of a database row nobody could reach from this sandboxed session anyway.
- **"STILL havent done the face outline thing... only one outline editor and its for a circular outline."** The real detected-face ellipse from the previous entry is still there, untouched — but its FALLBACK (no photo, detection not resolved yet, or no face found) was still a plain circle, and that fallback is what shows for however long detection takes to resolve per photo, or forever if TinyFaceDetector can't read a given render at all — exactly the risk flagged, and not yet confirmed either way live. Rather than wait on that, `drawPlayerHead.ts`'s fallback is now a real head-shaped OVAL (`r*0.82` wide, `r*1.04` tall) instead of a circle — so the outline never reads as "just a circle" regardless of whether detection ever succeeds on these images at all. The real detected ellipse still draws instead of this oval whenever it's actually known.
- Full 109-file suite and `tsc --noEmit` both clean (one existing test's hardcoded `offsetX === 0` expectation updated for the new real default of `0.03`, not weakened).

**15 September 2026 (cont. 2) — The outline now traces the real detected face, not a circle.**

- **The ask.** Direct, after actually testing the previous round's shipped circular-stroke outline live: "no the outline is still for the circle not the face." Third attempt at this exact feature (round one assumed alpha-cutout photos and traced a rectangle by mistake; round two correctly added a crop tool but reverted the outline to a plain circular stroke around the crop boundary — honest about the crop, still not the face). Asked directly rather than guessed a third time: reuse real face detection (feasible, since face-api.js/TinyFaceDetector is already a dependency of this codebase for tierlist thumbnail auto-centering) versus a quicker stylised oval that doesn't trace any specific photo. Chose real detection.
- **`lib/faceDetection.ts` gained `detectFaceBoxFromUrl`**, alongside the existing `detectFaceFromUrl` (which collapses a detection down to a single background-position point) — a new export that returns the real box as fractions (0-1) of the photo's own natural width/height, resolution-independent, reusing the exact same `ensureModelsLoaded`/`loadImage`/`resizeToCanvas`/`TinyFaceDetectorOptions` machinery the tierlist feature already has proven in production, rather than a second copy of any of it.
- **New `lib/star/faceOutline.ts`** — `ellipseFromBox` pads the raw detection box outward (face-api's box runs eyebrows→chin, snug around the inner features; a real head needs room above for the hairline and below for the chin/jaw — padded asymmetrically, more above than below, same reasoning `boxToFaceCenter`'s own existing +50% upward bias already uses) into a real ellipse. `getFaceOutline`/`requestFaceOutline` cache it (memory + localStorage, same two-tier shape the tierlist feature's own cache already uses) keyed by photo URL — detection is real, per-image work, never something a canvas render loop can afford to do live, so it's requested once per distinct photo (fire-and-forget, safe to call every single draw) and every draw reads back whatever's already known: a real ellipse, a real known "no face found," or genuinely not-yet-known — the last two both draw the plain fallback circle, never a blocked draw or a missing outline.
- **`drawPlayerHead.ts`** now transforms that ellipse through the exact same crop math (`sourceRect`) that places the photo itself, into the figure's own local head space, and strokes THAT instead of the circle whenever a real shape is known — a uniform (non-stretching) transform, so a real ellipse in the photo stays a real ellipse on screen, just scaled and moved with the crop. A light sanity clamp (0.3x–1.4x the head's own radius) guards against a genuinely bad detection producing a stray shape nowhere near the actual head — never seen live, so worth having. Falls back to the original plain circular stroke the instant there's no photo, no detection yet, or no face found — exactly the same graceful-degradation shape every other part of this feature already uses.
- **New `tests/star/faceOutline.mts`**: `ellipseFromBox`'s padding math against the real exported constants (a worked example, plus a general check that the vertical bias is genuinely asymmetric), and the cache/memoisation logic via `requestFaceOutline`'s injectable `detect` parameter — a real box resolving to the exact `ellipseFromBox` result, a second request for the same URL never re-detecting, a real "no face found" landing as a known `null` (distinct from "not yet known" `undefined`), a rejected detector promise resolving the same way rather than throwing, a cold read from localStorage needing no detection at all, and corrupt cache JSON never crashing. The actual `detectFaceBoxFromUrl`/face-api.js call itself can't be unit-tested here — needs a real DOM/canvas this environment doesn't have — same accepted limitation `drawPlayerHead` itself has always had.
- **The one real, honestly-flagged risk**: whether TinyFaceDetector reliably detects a face at all in SoFIFA's own game-rendered player portraits — it's tuned on real photographs, and these are stylised 3D-rendered faces, which is a genuinely different input than the tierlist feature's own real user-uploaded photos it was proven against. Every failure mode degrades gracefully (falls back to the plain circle, exactly like a photo that's still loading), so a bad detection rate would look like "outline mostly circles, sometimes hugs the face" rather than anything broken — but this could only be confirmed by actually looking at it live, which this sandboxed session can't do.
- Full 109-file suite (108 existing + the new `faceOutline.mts`) and `tsc --noEmit` both clean.

**15 September 2026 (cont.) — Faces/names toggles, and a real admin "global default" for the whole Face Editor.**

- **The ask, in full.** Requested directly, in one message, framed as "less than a minute": "its good this is customisable for the user (also in the settings add a toggle for player faces, a toggle for player names instead (like their names to be above their heads in clear text), and also an admin button to set the custom player face values as official and global default values. i want this to also be an admin tool like right now i found out that 3x size, 0.03 left/right, -2.04 left/right, like 10% circular crop is the perfect values for how it should look and i wanna set that as the default values so it naturally looks like that unless the user changes it (also have a reset to default button), also for some reason u removed the face outline that is necessary bring it back." Honestly: the two toggles were quick; the admin global-default mechanism is a genuine new piece of server-backed infrastructure (a table, a migration, an admin-gated API route, and a real "which value wins" precedence rule) — not a sub-minute change, whatever the framing. Built anyway, in full, this same session.
- **Two new `FaceStyle` fields** (`lib/star/faceStyle.ts`): `facesEnabled` (default `true`) and `namesEnabled` (default `false`). `facesEnabled` is a one-line gate in `drawPlayerHead.ts` — `hasPhoto` now also requires it, so turning it off falls every figure back to exactly the same backing-circle treatment a player with no photo on file already gets, never a blank head. Both are plain `!!` coercions in `sanitize()`, same pattern `showBacking`/`outlineEnabled` already use, so an OLD saved style with neither key present backfills to the real defaults rather than crashing — covered directly in `tests/star/faceStyle.mts`.
- **Names are real, not a stub** — wired into every figure that already carries a real `Identity` (teammates' follower/crosser/runners, opponents' defenders and keeper, and your own figure via the existing `playerLabel()` helper), reusing `footballer()`'s own `label`/`labelColor` mechanism in `CanvasMatch.tsx` — which, investigating before writing anything, turned out to be **fully wired but never once populated at any real call site**: a dead, unexercised code path with its own draw logic already sitting there. Its stored position (`py - r*1.02`) was also wrong on inspection — worked out by hand against the same local→world math `drawPlayerHead` itself uses, it sits BELOW the chin, not above the head, at every scale, including the original pre-editor default; nothing before this session had ever actually rendered a label to notice. Replaced with a real `headTopY` computed from the figure's own live `scale`/`offsetY` (so a label correctly stays clear of a head someone has pushed up or enlarged, not just the default position), plus extra clearance specifically on your own star-marked figure so a name can never sit through the star above your head. Also added, matching the same math, to the keeper's own bespoke figure (the one body that doesn't go through `footballer()` at all) — deliberately NOT tracking his live dive-lean rotation/crouch offset, an honest, small simplification: exact while he's idle, a few px off only during the one dive-frame that already has your full attention anyway. The two legacy top-down dribble-scenario figures (`USE_FIRST_PERSON_DRIBBLE = false` fallback path) were left alone, matching this whole cycle's own established precedent for that deprecated path.
- **Settings gets the two toggles directly** (`SettingsScreen.tsx`'s existing "Player Graphics" panel) — plain checkboxes, save immediately, no need to open the full editor just to flip one switch.
- **The admin mechanism — the actual novel piece.** New `star_face_style_default` table (`supabase/migrations/star_face_style_default.sql`, PENDING — see table above), one row, a JSONB `style`. New `/api/star/face-style-default` route, GET public / POST admin-only via `isAdmin()` — same shape as `star_lineups.sql`/`route.ts`, the direct precedent read before writing either file. The Face Editor gets a new **"Set as Global Default (Admin)"** button that POSTs whatever is live in the editor right now — deliberately capturing the exact real numbers rather than me hand-parsing the prose values, which sidesteps the one genuinely ambiguous part of the request (see below). Precedence, and why it's safe: a device that has ever saved its own style (`hasFaceStyleOverride()`, new) always keeps using it — a global default only ever reaches a device that's NEVER customised anything, and it's re-checked (never cached into the device's own override key) so a device that's never touched a slider keeps tracking whatever the admin sets NEXT too, not just whatever was live the first time it loaded. **"Reset to Default"** now targets the live global default when one has loaded, falling back to the hardcoded `DEFAULT_FACE_STYLE` otherwise — resetting means "what everyone else sees," not always the code's own original constant.
- **The given numbers, and the one real ambiguity in them.** The migration seeds one row so the default is live immediately once it's run, read as `scale: 3, offsetX: 0.03, offsetY: -2.04` (the repeated "left/right" almost certainly means "up/down" for the second value — the editor's own two sliders are literally labelled "Left / Right" and "Up / Down") and `crop.zoom: 1.1` for "10% circular crop" (a genuinely ambiguous hand-typed percentage against a `[1,4]` zoom range where 1 means "no crop at all" — read as a modest trim, not a heavy zoom). This is a best-effort reading, explicitly flagged as such in the migration's own comment, and `ON CONFLICT DO NOTHING` so it only ever fills an empty table — the real, exact numbers are whatever gets POSTed the first time an admin opens the editor (with their own already-dialled-in local style showing, since local overrides always win) and presses the button. That is the one precise, unambiguous path to "exactly what I found looks perfect," and is the one I'd point at first.
- **The outline was never actually removed.** Re-read `drawPlayerHead.ts`'s own outline block directly — `if (style.outlineEnabled) { …stroke… }` is fully intact, and `DEFAULT_FACE_STYLE.outlineEnabled` has always been `true`; nothing in this whole session's work touched it. Two real, honest possibilities instead, both worth checking rather than picking one: (1) this device's own locally-saved style has `outlineEnabled` off — a local override always wins over any default, including the new global one, so it would look "gone" regardless of what either default says; the Outline checkbox in Settings → Player Graphics settles it directly. (2) The outline's stroke width has never scaled with `style.scale` — only with the whole figure's own radius — so the SAME absolute stroke around a head grown from the shipped 1.4x default to the requested 3x reads as visibly thinner relative to the now-much-bigger circle, even fully enabled. Not changed this session (not what was asked, and a real behaviour change for every existing user, not just this default) — flagged here rather than guessed at silently.
- Full 108-file suite and `tsc --noEmit` both clean, including new coverage in `tests/star/faceStyle.mts` for both new fields (default value, round-trip, old-data backfill, non-boolean coercion) and for `fetchGlobalDefaultFaceStyle()` itself (a real `fetch()` boundary, faked the same way `localStorage` already is here: a real style response, a `null` style — migration run but nothing set yet — a non-ok response, and a rejected fetch all resolve to the right thing without throwing). As with every visual change this whole cycle, none of this has been seen live — `/star-dev` needs Google sign-in, which this sandboxed session can't complete — confidence rests on the type-check, the new test coverage, and re-deriving the label position math by hand against `drawPlayerHead`'s own real transform, not a screenshot.

**15 September 2026 — The alpha-outline assumption was wrong; replaced with a real crop tool, and the circle clip is back.**

- **The ask, and the real information it carried.** "some of the picture is in it that i dont want in it, like the neck and a bit of the shirt, i cant customise it enough to make it perfect. more customisation please, better editor still, i wanna be able to crop some bits out, have like a circular crop thing, and whatever else may help." This settled, for real, the one thing flagged as an unconfirmed assumption in the previous entry: real player photos are plain rectangles — head, neck, and a bit of shirt — not alpha-cut-out headshots. The "trace the photo's own alpha shape" outline built on that wrong assumption was never going to do anything useful against an opaque rectangle (recolouring every "visible" pixel of an opaque image recolours the WHOLE rectangle, so the outline it produced was tracing a rectangle, not a face) — so this isn't a tuning pass on top of that design, it's a real correction of it.
- **The actual fix: a genuine crop, reusing geometry that already existed rather than inventing new geometry.** `PortraitPicker.tsx` (Settings → Photo, your own picture) already has exactly this problem solved — `lib/star/portrait.ts`'s `CropView`/`coverScale`/`clampOffset`/`sourceRect`/`initialView` are the real pan-and-zoom math a crop tool needs. `FaceStyle` (`faceStyle.ts`) gained a `crop: CropView` field — ONE shared crop, applied to every real player's photo alike, same spirit as every other field on it — and `drawPlayerHead.ts` now calls `sourceRect(style.crop, …)` to pick which rectangle of the SOURCE photo actually gets sampled, via `drawImage`'s 9-argument form, before it's clipped to the head circle.
- **The circle clip is back, and the outline is a plain stroke again — correctly, this time.** Once cropping is the real answer to "cut the neck out," clipping the (now correctly cropped) result to a circle is exactly right, and a plain circular stroke traces that same circle honestly — no silhouette compositing, no offscreen canvas, no per-(photo,colour) cache to maintain. Deleted `silhouetteFor`/`stampOutline`/the `WeakMap` cache outright rather than leaving unused, wrong-premise code sitting in the file.
- **New "Crop Photo" section in the Face Editor**, styled and behaved to match `PortraitPicker`'s own established crop stage as closely as reasonable — a fixed-size viewport (`CROP_VIEWPORT`, 256px, its own constant since it's a different concern from `PORTRAIT_SIZE`), drag-to-pan, a zoom slider (zooming about the viewport's own centre, matching `PortraitPicker.setZoom`'s exact reasoning for why — without it the face slides out of frame every time the slider moves), and a circular "what's kept" vignette drawn with a single CSS trick (a circular div with `box-shadow: 0 0 0 9999px rgba(0,0,0,0.6)` — the shadow's own huge spread fills everything outside the circle, no canvas overlay math needed). The crop snaps to a sensible centred `initialView` the first time a real photo loads, but only while it's still genuinely untouched (exactly `{zoom:1,x:0,y:0}`) — the moment a real drag or zoom moves it even slightly, that auto-centring permanently stops firing, on any future player switch too, so it can never fight an edit you've already made.
- New `tests/star/faceStyle.mts` coverage for the crop field: it round-trips exactly, an absurd stored value clamps to something finite and in-range rather than corrupting, and an OLD save from before `crop` existed (no such key at all) still loads cleanly with the real default crop filled in rather than crashing or leaving `undefined`.
- Full 108-file suite and `tsc --noEmit` both clean. As with every visual change this whole cycle, none of this has been seen live — the crop math itself is the exact same, already-in-production geometry `PortraitPicker` already uses for a different photo, which is real confidence, not a fresh guess; the new CSS vignette trick and the drag feel are the two genuinely new pieces worth a real look first.

**14 September 2026 (cont. 5) — More editor freedom, and the outline redefined to trace the photo's own shape rather than a circle.**

- **The ask, two parts.** "let me put the faces higher and bigger (give me more freedom to customise)" — the previous entry's ranges were too tight. And a real clarification on what "outline" was supposed to mean, quoted in full since it changed the actual design: "an outline around the FACES (not the image file which may include transparent area), like i wanna see a face with the outline going around every pixel of the face, thats the outline i wanna be able to toggle and customise, as well as the normal face one." That last clause — "as well as the normal face one" — confirmed the backing circle stays exactly as it was, as its own separate toggle; only what "outline" itself draws needed to change.
- **More room, both places at once.** `lib/star/faceStyle.ts` — new `FACE_SCALE_RANGE` (`[0.5, 5]`, up from `[0.5, 3]`) and `FACE_OFFSET_RANGE` (`[-3, 3]` head-radii, up from `[-1, 1]`), exported so the clamp logic, the editor's own sliders, and its drag-to-reposition clamp all read from the one real source rather than three copies of a number that could drift apart again. `tests/star/faceStyle.mts` gained a floor check (scale reaches at least 5x, offset at least ±3) — a floor, not an exact pin, so a future widening still passes without the test needing an edit; only a narrowing would ever fail it.
- **The outline redesigned — it was never actually about the photo before.** The first version stroked the CIRCULAR CLIP boundary — a ring drawn over the photo, blind to what the photo itself actually showed. Real player photos (SoFIFA's own renders) are typically a head/bust cut out on a transparent background, not a filled rectangle — exactly the "image file which may include transparent area" called out directly. `drawPlayerHead.ts` (`lib/star/`) rewritten: a real photo no longer clips to a circle at all — it draws at its own natural alpha shape, with the backing circle (unchanged, still its own toggle) showing through anywhere it's actually transparent — and the outline is a genuine silhouette trace: `silhouetteFor()` recolours every visible pixel of the photo to the outline colour via `globalCompositeOperation = "source-in"` (the standard "solid silhouette of a sprite" technique), and `stampOutline()` stamps that silhouette at 12 points around a small ring before the real photo draws on top — the real photo covers the middle of its own dilated shadow, leaving only the newly-exposed ring visible, which reads as an outline hugging the actual face. A player with no photo has no real alpha shape to trace, so that case keeps the original plain circular stroke — there's nothing dishonest to fake there.
- **Cached, because a real match calls this every frame for every figure.** The recoloured silhouette is genuinely expensive to build (a full offscreen `drawImage` + composite pass) but only ever needs building once per (photo, outline colour) — a `WeakMap<HTMLImageElement, Map<colour, HTMLCanvasElement>>` inside `drawPlayerHead.ts` keeps one per real photo actually seen, built at the photo's own natural resolution and rescaled cheaply at draw time via `drawImage`'s own destination sizing — never rebuilt per frame, per figure, or per on-screen size, even though the SAME photo draws at genuinely different pixel sizes depending on how far from camera a given figure is.
- Full 108-file suite and `tsc --noEmit` both clean — nothing in the clamp/range widening or the outline rewrite touches anything the existing suite exercises, and the new `faceStyle.mts` floor check passed on the first run. As with every visual change this cycle, none of this has been seen live — the alpha-silhouette technique in particular is a real, standard canvas trick but has real edge cases (whether SoFIFA's renders genuinely have a transparent background the way assumed here is itself an inference, not confirmed against a real fetched file this session) worth checking first, before trusting it fully.

**14 September 2026 (cont. 4) — A proper Face Editor replacing the size slider, and your own face fixed.**

- **The ask.** The scale-only slider from the previous entry was reported back directly as not enough: "no not a slider, i want a proper well made player graphic editor in the settings... the user needs to be able to select a player, see how they look, move the face around, adjust the size/scale, edit things like whether or not theres a normal 'face' circle behind the face picture, add/edit an outline with colour and size and thickness etc, i need to be able to edit all this so that every player looks exactly how i envision." Read as: ONE shared style, applied to every real face uniformly — not hundreds of individually-configured players — with a real WYSIWYG tool to dial it in, previewed against whichever real squad player you pick to check it against.
- **`lib/star/faceStyle.ts`** replaces `faceScale.ts` outright (deleted, along with the now-superseded `FaceScalePanel.tsx`). A single `FaceStyle` object — `scale`, `offsetX`/`offsetY` (in units of the head's own base radius, so a shift scales sensibly with figure size), `showBacking` + `backingColor`, `outlineEnabled` + `outlineColor` + `outlineWidth` — still localStorage-only for the same reason as before (a personal display preference, nothing to sync or migrate). `loadFaceStyle()` seeds `scale` from the OLD slider's saved value if the new key isn't present yet, rather than silently discarding whatever the user had already dialled in; everything is clamped on save so a corrupted or hand-edited value can't produce a broken draw call. New `tests/star/faceStyle.mts` covers the round-trip, the clamping, the old-slider migration, and that malformed stored JSON falls back to the real default rather than throwing.
- **`lib/star/drawPlayerHead.ts` (new) — the one function that actually draws a head, everywhere.** Extracted from what was previously separate, near-duplicate code inside `footballer()` and the keeper's own bespoke figure (`CanvasMatch.tsx`) — both now call this one function, and so does the editor's own live preview canvas. This was the one real architectural decision worth getting right: a "preview" built from a second, approximate drawing routine could quietly disagree with what a real match actually shows, which would defeat the entire point of a WYSIWYG editor. `showBacking` is honoured only when there's a real, loaded photo to show instead — a player with no photo on file (or one still loading) always gets the backing fill regardless of the toggle, so a head can never go invisible. Unified the keeper's own slightly different base outline thickness (`KR*0.09`) into the same formula the outfielder used (`r*0.10`) — a difference small enough that both already rounded to the same 1px floor on most phone screens, and worth it for one real shared function instead of two that could drift.
- **New `components/star/FaceEditorScreen.tsx`** — its own screen (new `"face-editor"` `StarPhase`), reached via a real "Open Editor →" button in Settings' new "Player Graphics" panel (replacing the old inline slider). A player picker (your real squad, defaulting to whoever has a real photo first), a live preview canvas you can genuinely DRAG the face around on (pointer events converting drag distance directly into `offsetX`/`offsetY`, at a ratio that makes one head-radius of drag equal exactly 1.0 of offset), plus sliders/colour pickers/checkboxes for everything else, a Reset button, and autosave on every change — no separate Save step, matching this game's other toggle-shaped settings. The body drawn around the head in the preview is a simple, NOT shared, representative sketch (legs/shorts/arms/shirt in the player's own club kit) — deliberately not extracted like the head was, since the body isn't configurable at all and doesn't need to be pixel-identical to a real match figure, only recognisably a footballer.
- **A real, reported gap fixed: your own face never showed up at all.** Not a bug in the strict sense — the previous faces pass explicitly scoped the player's own figure out ("the star marker already answers 'which one is me'"), but that reads as broken once every OTHER figure has a real photo and yours doesn't. `footballer(...)`'s call for `sc.player` now passes `face: getFaceImage(careerRef.current?.player.portrait)` — the same portrait already settable in Settings → Photo (`PortraitPicker.tsx`), previously only reaching Player of the Month graphics. Worth knowing: that value is a `data:` URL (client-side only, "stored on this device", per `PortraitPicker`'s own doc), not an `http(s)` one like every other player's photo — `getFaceImage`'s cache and draw path handles it identically either way; its `onerror` retry (which appends a query string, meaningless on a data URL) simply never has anything to fire on, since a data URL either decodes immediately or not at all.
- Full 108-file suite and `tsc --noEmit` both clean. As with every visual change this whole cycle, none of this has been seen live — confidence rests on the type-check, the new `faceStyle.mts` coverage, and reading the canvas transform math by hand; `drawPlayerHead` itself can't be unit-tested without a real `CanvasRenderingContext2D`, which this test environment doesn't have. The drag interaction and the colour pickers in particular are worth a real look next time someone's signed in.

**14 September 2026 (cont. 3) — Real feedback from the first live look at faces: a user-adjustable size dial, a photo-refresh button, and the keeper freezing on a shot he had no chance of reaching.**

- **First real, live confirmation the faces feature actually merged and deployed correctly** — a genuine screenshot from the real site, faces genuinely rendering on real figures. Also surfaced a real process mistake: the faces commit had been pushed onto the OLD `Leo` branch AFTER its PR (#482) had already merged, so it could never have shown up in that PR — exactly the "already merged, must restart the branch" case this session's own instructions call out. Fixed by fast-forwarding local `Leo` to the current `main` and opening a fresh PR (#484); the lesson (check whether the designated branch's PR has already merged BEFORE pushing follow-up work, not after) is now applied at the start of this entry's own work too.
- **Faces too small to actually tell players apart — reported directly, with a phone screenshot.** `lib/star/faceScale.ts` (new): a personal DISPLAY preference — deliberately localStorage-only, not on `CareerState`, since it's a per-device rendering choice with nothing to sync or migrate (same idea as `CanvasMatch.tsx`'s own pre-existing `"star-match-muted"`/`"star-match-speed"` keys). `FACE_SCALE_DEFAULT` (1.4) is a real, immediate bump over the old unscaled baseline; the user then gets a real slider (new `FaceScalePanel.tsx`, wired into Settings) to dial it in themselves — asked directly for this rather than another guessed constant, since there's no visually-verified "correct" size without a live look at an actual phone. Applied to `headR`/`kHeadR` specifically (`footballer()` and the keeper's own separate head-drawing block in `CanvasMatch.tsx`), deliberately NOT to `r`/`R`/`KR` (the whole figure) — scaling the whole body would also grow the keeper's own on-screen footprint, which an earlier session deliberately kept small so he never blocks the shot you're watching at. A player with no real photo on file still gets the bigger plain circle too, so the pitch never shows two different head sizes side by side depending on who happens to have a photo yet.
- **A real, root-caused bug: an old save's faces mostly blank, a new save's almost all populated.** Investigated rather than guessed. `shouldUpgradeSquad` (`realSquad.ts`) and `shouldUpgradeLeagueSquads`/`shouldUpgradeExternalSquads` (`leagueSquads.ts`) — the checks that decide whether a career's cached squad data is stale enough to silently re-fetch on load — are "good enough, once" heuristics: `shouldUpgradeLeagueSquads` specifically stops re-checking the moment image coverage clears a 50% bar, and `shouldUpgradeSquad` only ever asks "does ANYONE have a `sofifaId`" (real vs generated), never anything about photos specifically. Once a save clears whichever bar applies, it never re-fetches again automatically — so a photo added to Supabase Storage AFTER that point never reaches an existing save on its own, while a brand-new save (fetching fresh, right now) gets full current coverage. New **"Refresh Player Photos"** button in Settings (`RefreshPhotosPanel.tsx`, new) — `app/star-dev/page.tsx`'s new `handleRefreshPhotos` re-fetches the player's own squad, the rest of the division, and the wider world, unconditionally (no staleness gate — the whole point is a user override), merged in via the exact same stats-preserving functions the automatic background path already uses (`mergeSquadStats`, `mergeLeagueSquadStats`), so a manual refresh can never reset this save's own goals/assists tallies. Old save kept exactly as playable and exactly as-is except for the refreshed photos, per the direct ask ("i want to keep my old save though").
- **A real, reproducible bug: the keeper freezing mid-dive on a shot that clearly beat him.** Reported directly, with the exact mechanism named: "the keeper should still continue its dive/animation... most times hes barely moved an inch if i score a quick powerful shot he cant get to... it will look better if the ball goes in and hes still mid dive and falling to the ground as the goal animation pops up." Root cause, found by tracing the render loop rather than guessing: `stepKeeper` was only ever called from the loop's `"aim"` branch (patrolling) and its `"flight"` branch (the live substep loop that also calls `stepBall`) — the MOMENT `resolveOutcome` fires (the instant `stepBall` returns a result) it sets `phase` to `"result"`, and on the very next frame the entire substep block — `stepKeeper` included — stops running. A shot that beat him outright (this session's earlier keeper-dive rework: `pendingDone` true, real capped-speed travel toward `targetX` still in progress) simply stopped being simulated at all at that exact instant, freezing him wherever he happened to be — often having barely moved — while the goal/result UI had already appeared. The BALL never had this problem: three existing blocks right below `resolveOutcome`'s effect already keep it moving post-resolution (into the net, settling after a save, flying on past the bar) specifically so it's seen rather than frozen. Fixed with a fourth block of the same shape — the keeper keeps calling `stepKeeper` through the `"result"` phase too, gated only on `!keeper.done` so it stops once he's actually arrived. `stepKeeper` reads only the keeper's own fields, never the ball, so this is exactly as safe post-resolution as it already was during flight. Not independently unit-testable the way the rest of this session's engine work was — this is a React-component render-loop wiring fix, not a change to any function the `tests/star/*.mts` suite calls directly, so confidence here rests on tracing the actual call graph, not a new measurement.
- Full 107-file suite and `tsc --noEmit` both clean — nothing in `stepKeeper`, `mergeSquadStats`, or `mergeLeagueSquadStats` itself changed, only where/when they're called from, so no existing test needed a single assertion touched. As with everything else in this whole cycle, none of today's three fixes has been seen live — confidence rests on the type-check, the full suite, and tracing the exact code paths involved (the render-loop gating for the keeper fix in particular), not a fresh screenshot. Worth a real look next time there's one.

**14 September 2026 (cont. 2) — Real player faces, composited onto every figure on the pitch; the stats work is next.**

- **The ask, and the sequencing.** Requested directly, in one message: real in-game stats so players stop being interchangeable — a real goalkeeper rating (Courtois harder to beat than a random keeper), real teammate finishing (Haaland scores more, and can even curl a shot around a defender or the keeper from a tough angle), and real defender quality (Van Dijk blocks better than a random centre-back) — but with an explicit precondition: faces first, because there's no way to verify any of it without being able to tell the players apart, and a name label above the head was ruled out as ugly. Explicit instruction: build all of it now only if it's genuinely, certainly all easy; otherwise say so honestly and default to faces-first-then-stats.
- **The honest answer: not all of it was equally easy, so faces went first, alone.** Investigated all four pieces before writing any code:
  - **Teammate finishing turned out to already be real**, not something to build. `launchReceiverShot` (`canvasEngine.ts`) already reads `receiver.skill`, which is already set from the real receiving player's actual `overall` the moment the ball reaches him (`scenario.receiver.skill = clamp(r.who.overall + gaussian(rng) * 6, 0, 100)`), and already uses it to drive aim precision, how well he reads the keeper's position, and execution spread. Haaland already finishes better than a reserve centre-back, live, today.
  - **Curl is a small, low-risk addition, not a new mechanic.** The engine already has real, tuned Magnus-style curve physics (`CURL_K`, consumed in `stepBall`'s integration, already used elsewhere — free kicks). `launchReceiverShot` currently only sets a small flat, skill-independent `spin` value. Scaling that by finishing quality is a genuinely small follow-up, not a new physics system.
  - **Keeper individual rating is small**, as already flagged in the entry above — `keeperStrength` is one existing dial, currently fed the whole opposing club's average instead of the real keeper's own rating.
  - **Defender blocking quality is genuinely NOT small — the one piece flagged back to the user rather than built.** Checked the actual tackle/block code (`canvasEngine.ts`, "A defender gets to it"): it is pure geometry — `if (Math.hypot(d.x - ball.pos.x, d.y - ball.pos.y) < reach) { …; return "blocked"/"tackled"; }` — no roll, no skill check, nothing to extend, unlike keeper saves or finishing which already had a quality dial before today. `Defender` also carried no identity at all before this session. Building "Van Dijk blocks better" means designing a whole new probabilistic layer from nothing and calibrating it as carefully as the keeper dive rework in the entry directly below this one — a comparable-sized job of its own, not a quick add. Told to the user plainly rather than folded into "now."
- **What was actually built: real faces on every figure, teammates and opponents alike.**
  - **`Identity`** (`canvasEngine.ts`) gained an optional `face?: string` — a real photo URL when the database has one, same "absent for a generated squad" caveat `overall` already carries. Nothing reads it for anything except drawing him.
  - **`Defender` and `Keeper`** (`canvasEngine.ts`) each gained an optional `who?: Identity` — until today only `Runner`/`Follower`/`Receiver` (your own attacking side) carried a real identity at all; the men marking you and the opposing keeper were pure geometry with no connection to any real player, not even a name. Purely additive (both optional) and purely cosmetic — confirmed nothing reads either field for tackle or save resolution.
  - **`idOf`** (`lib/star/lineup.ts`) — the one conversion point every real squad player already passes through to become an `Identity` — now carries `face: p.imageUrl` alongside `overall`. This is the same real, self-hosted `player-portraits/{sofifa_id}.png` pipeline described above (PL Draft — Data Status), already flowing into the pre-match team sheet; now it also reaches the match itself.
  - **New `castDefence`** (`lib/star/lineup.ts`) — the other end of what `castScenario` already does for your own side. The real opposing starting XI (with real faces) was already sitting in `CanvasMatch.tsx` as `oppXI` (`opponentStartingXI`, teamsheet.ts) for opponent-goal crediting, just never connected to the men actually drawn on the pitch. `castDefence` assigns the real GK to `Keeper.who` and distributes the real outfield XI across `Defender[]`, closest-to-goal matched to closest-to-goal on both sides — deliberately simpler than `castScenario`'s nuanced position-preference matching, since nothing is credited off a `Defender` or a `Keeper` the way a goal is credited off a `Runner`, so there's no wrong-man-credited bug to guard against, only a face to put on the right kind of figure. A genuine no-op with nothing to scout (international fixture, a side too thin, the sandbox with no career at all) — every figure just keeps drawing as the plain shirt it always has.
  - **`footballer(...)`** (`CanvasMatch.tsx`) — the one shared function that draws every outfield figure — gained an optional `face?: HTMLImageElement`. The head was always a flat-colour circle (`ctx.arc(0, -r*0.76, r*0.26, …)`, filled and stroked); a real photo now draws clipped to that exact same circle, on top of the same fill, before the same outline stroke — so a missing or still-loading photo leaves today's plain head exactly as it always looked, never a gap. The keeper has his own separate, bespoke hand-drawn figure (dive lean, gloves, the lot) rather than going through `footballer` at all, so his head got the identical treatment duplicated inline rather than refactored to share code mid-session.
  - **New `getFaceImage`** cache (`CanvasMatch.tsx`) — a `Map<url, HTMLImageElement>`, one real `Image()` load per distinct photo URL, reused every frame. Deliberately lighter retry than the proven ball-loading pattern right above it in the same file (one retry with a cache-busting param, not a five-attempt backoff): a slow or missing face just leaves that one man on the plain circle every figure with no real identity already falls back to — an ordinary state here, not a broken one, and a squad can be twenty-plus distinct real photos in one match.
  - Threaded through every `footballer(...)` call site with a real identity to draw from: the poacher/follower, the runner(s) a pass is aimed at, opponent defenders, and — new — the one real "crosser" among the otherwise-decorative `sc.teammates` array (`sc.teammates[0]` specifically; the rest were always decoration and stay that way, confirmed via `castScenario`'s own `sc.crosser = claim(...)` off exactly that element). The player's own figure (already marked with a star) and the old top-down dribble scenario's chasers were deliberately left alone this pass — narrower scope, lower risk, and the star marker already answers "which one is me" without needing a photo.
- **New test coverage, not just a type-check.** `tests/star/lineup.mts` gained two sections: one confirms a real face survives `idOf`'s conversion intact across 1,500 real scenario builds and that a man with no photo never has one invented for him; the other exercises `castDefence` directly — the keeper and every defender get a real match, the closest-to-goal pairing is checked exactly (not just "some assignment happened"), a real face carries through on both ends, and three empty/partial-sheet shapes (`null`, `undefined`, `[]`, and an outfield-only sheet with no GK) are all confirmed genuine no-ops. One real bug was caught building this test, not shipped: indexing `SQUAD[0]`/`[1]` for two test players silently picked the generated goalkeeper (`generateSquad` always builds one first) and a man `castScenario` correctly never gives an outfield shirt, reading as "the feature is broken" (zero sightings in 1,500 tries) until the test itself was fixed to pick real outfield players by position the same way this file's own existing tests already do.
- Full 105-file suite and `tsc --noEmit` both clean — a purely additive, purely cosmetic change; no existing test needed a single assertion changed. As with every canvas-engine change this cycle, `/star-dev` needs Google sign-in, which this sandboxed session can't complete, so **none of this has actually been seen on screen** — confidence here rests on the type-check, the new measured test coverage, and working through the canvas transform math by hand (the clip-to-circle happens in the figure's own local, already-rotated coordinate space, same as the plain fill it replaces), not on a live look. Worth a real check next time someone's signed in, before trusting the visual result completely — in particular whether a photo reads clearly at the size these figures actually draw at on a phone.
- **Deliberately not attempted**: rotating a face to stay upright independent of the figure's own `facing` rotation. The circle clip itself is rotation-invariant, but correctly counter-rotating just the photo without also re-deriving where its centre lands in the un-rotated frame is real trigonometry with a real chance of drawing the photo in the wrong place rather than merely tilted — worse than the alternative. Left simple and safe instead: the face rotates with the whole figure, exactly like the shirt and limbs already do. Most figures use `facing` for a modest turn, not a wild spin, so this is expected to read as a minor cosmetic simplification rather than a visible problem — flagged honestly since it's the one part of this not backed by a live look.
- **Next up, per the user's own stated order**: once faces are confirmed to look right, keeper individual rating and the finishing curl are both small, ready to go quickly. Defender blocking quality stays its own separate, larger piece — not started.

**14 September 2026 (cont.) — The keeper genuinely dives for real shots now, including ones he has no chance of reaching.**

- **The ask.** Requested directly, in detail: the keeper should never stand still and let a shot in, and should never "teleport" to a ball that was close enough — he should visibly dive, sometimes fail to reach it (especially toward the corners, which should now be a noticeably better place to aim), and, once he does get there, have a small genuine chance of spilling/fumbling it in. The one hard constraint: none of this should make the game measurably easier or harder to score against than it already was — only the top corners should visibly change.
- **`lib/star/canvasEngine.ts` — `keeperCovers` replaced with `keeperAttempt`.** The old function was a single hard cutoff (`distance < radius`) decided the instant a shot reached the keeper's line, with an animation stitched on afterward purely to match whichever way it had already gone (a save teleported his `x` straight to the ball; a goal left him standing exactly where he already was). `keeperAttempt` keeps the same distance-vs-reach comparison for the CERTAIN cases (nothing about the difficulty curve moves for a shot well inside or well outside his reach) but turns the narrow band right at the edge — the last ~fifth of his reach either side of the old cutoff — into a genuine, symmetric probability roll instead of a knife-edge. His `x` no longer snaps to the save point; he now travels there over real frames using the exact same capped-speed "chase a spilled rebound" machinery `stepKeeper` already had (a new `Keeper.pendingDone` defers marking him `done` until that real travel actually arrives, instead of freezing him the instant the outcome was decided). `resolveKeeper` gained a new, deliberately rare goalkeeping-error branch — even a keeper who gets there can still fumble it in, scaled by how much of a stretch the save was.
- **A real, reported bug in the first version, fixed the same session**: told directly afterward that a shot with genuinely no chance of being reached (miles from him, top corner from too tight an angle, etc.) still left him standing completely still — the first cut had kept a "not even worth trying" distance gate (`KEEPER_ATTEMPT_MULT`), on the reasoning that a real keeper wouldn't bother diving for a hopeless ball. Overruled directly: an unstopped goal has to look like it beat somebody or it reads as undefended, not scored, however far out the placement was. The gate is gone entirely — there is no longer any distance past which he simply does not dive; every shot that reaches his line gets a real, full attempt, capped only by how far he can physically travel toward it. Confirmed this does not touch difficulty at all: the gate only ever controlled whether the ANIMATION played, never whether `resolveKeeper` fired or what the reach-roll returned, so removing it is a pure presentation change.
- **Calibration was measured, not assumed, and two real mistakes in the measurement itself were caught before trusting it** (both documented inline in `tests/star/keeperDive.mts`): a first reading of "does this change the aggregate concede rate" looked like it had made the game noticeably easier, until it turned out most of that gap was a completely different, pre-existing, unrelated mechanic (a save that stays live being followed in and scored a few frames later) being miscounted as if this change had caused it — fixing the comparison to the shot's own first-contact outcome only showed the real effect was small and in the other direction, which the new `KEEPER_QUALITY_BIAS` constant corrects. Final measured numbers: aggregate concede rate within half a percentage point of the old formula's own prediction on the same real shots; concede rate on a controlled sweep from dead centre to the extreme corner climbs from ~13% to ~45%, which is the one thing that was supposed to move.
- Full 105-file suite and `tsc --noEmit` both clean. As with all canvas-engine work this cycle, `/star-dev` needs Google sign-in, which this sandboxed session can't complete, so none of this has been checked in a live browser — confidence rests on the type-check, the full suite, and the dedicated new measurements in `keeperDive.mts`.
- `lib/star/canvasEngineTest.ts` (the `/star-match-dev` sandbox fork) only picked up the new `Keeper.pendingDone` field, for type compatibility with the shared `lib/star/lineup.ts` helper both engines call through — its own save logic is untouched, same as every other engine change not deliberately ported there.
- **Also asked, and answered but not built** (at the time): how hard would real per-goalkeeper stats (and eventually a real face) be. Real per-player data (name, rating, self-hosted photo) is already fetched for every match for other purposes (team sheets etc.) but never reaches the keeper — today `keeperStrength` is the whole opposing CLUB's average strength, not the actual goalkeeper's own rating, fed in at `app/star-dev/page.tsx`'s one `keeperStrength={oppStrength}` call site. Swapping that for a real per-GK lookup is a small, contained follow-up — his reach already scales with that one number today. A real face rendered onto his body was flagged as a separate, bigger piece — and built the same day; see the entry above.

**14 September 2026 — Multiple save slots for the star career game, built to degrade to zero risk rather than block on a migration this session can't run.**

- **The ask, and the one real risk in it.** Requested directly: let a player keep several careers and switch between them from Settings, with an explicit instruction to flag it first rather than build it if it could put the existing single save at risk. The one genuinely delicate part, exactly as scoped in an earlier conversation: `star_careers` is one row per account (`UNIQUE (user_id)`) — turning that into several rows needs a real schema change against the LIVE table every current player's save is in, and (per this file's own "Pending Setup" note) a remote/cloud session like this one cannot reach Supabase to run it directly. Rather than stopping there, the whole feature was built to work fully and safely with the migration still unrun, and to upgrade itself automatically the moment it is — the same shape as `high_potential.sql`/`world_class_potential.sql`.
- **`lib/star/storage.ts` — `MAX_SAVE_SLOTS` (3) and `slotScope`.** Slot 1 is deliberately defined as just `accountScope` itself — byte-identical to the one key every existing save has always lived under, and to what every other call site (`app/star-match-dev/page.tsx`'s own `loadCareer(user.id)`, `tests/star/career.mts`) already passes. That is what makes this safe rather than a migration: an account that never opens the new saves screen reads and writes the exact same localStorage key it always has. Slots 2/3 are new, additive, empty-until-used keys built from it (`accountScope#slot2`, `#slot3`). New `listSaveSlots()` (a local-only summary — club, player name, season, star rating — for the Settings list) and `loadActiveSlot`/`saveActiveSlot` (which save was last open, defaulting to 1). `loadCareer` was split into a new internal `loadCareerRaw` so peeking at slots 2/3 for the list can never accidentally trigger the pre-existing legacy/anonymous-save claiming logic meant only for slot 1.
- **`supabase/migrations/star_career_slots.sql` (new, PENDING)** — adds `slot SMALLINT NOT NULL DEFAULT 1` (Postgres backfills every existing row to slot 1 on its own; nothing is rewritten by hand) and swaps the old `UNIQUE (user_id)` for `UNIQUE (user_id, slot)`, finding and dropping the old constraint by what it actually covers rather than assuming its auto-generated name. Idempotent, with a verify query. RLS is untouched on purpose — every existing policy is a row-level `auth.uid() = user_id` check that already covers any slot with no change needed.
- **`app/api/star/career/route.ts` made slot-aware with automatic fallback**, the same technique `league-squads/route.ts` already uses for `high_potential`/`world_class_potential`: GET/POST/DELETE all try the slot-aware query first; if it errors (the migration hasn't run — Supabase fails the WHOLE query when a column doesn't exist, not just the missing part) AND the request was for slot 1, it retries with the exact pre-slots query shape this route has always used. Slot 1's cloud save is therefore provably unaffected whether or not the migration has been run. A slot other than 1 simply has no cloud row yet if it hasn't — indistinguishable from "empty slot," which the client already handles by falling back to localStorage — so nothing breaks, it just stays local-only (same device) until the migration runs, at which point it starts syncing with no further deploy.
- **`app/star-dev/page.tsx`** — the mount effect's ~130-line local-vs-cloud-reconciliation-and-resume logic was extracted, unchanged, into a reusable `loadCareerIntoState(slot)`, now also used when switching saves; a new `resetTransientState()` clears every piece of in-flight UI state (a pending vote, press question, transfer offer, goal replay, …) that belongs to whichever career is on screen, so switching away can never leak state into the save that replaces it. New `handleSwitchSave`/`handleStartNewInSlot`/`handleDeleteSave`, plus a `flushCloudSave` that sends the outgoing save to the cloud immediately on switch rather than trusting the existing 3 s debounce to survive it. `handleFullReset` (used by the retired-career "New career" screen, untouched) now scopes to the active slot.
- **`components/star/SaveSlotsPanel.tsx` (new)**, wired into Settings in place of the old single "New career" button: all three slots always shown, the active one marked "Playing", an empty one offers "Start career", an occupied one offers "Switch" and "Delete" (worded "Delete & start over" for the active slot — the same destructive action the old button was, just per-slot now).
- `tests/star/saveSlots.mts` (new): slot 1 is provably the exact pre-existing key; an old, unslotted save reads back through slot 1 with no migration step; slots are fully independent of each other and of another account's same-numbered slot; a pre-slots legacy save is claimed into slot 1 and never mistakenly picked up while merely listing slots 2/3; the active-slot pointer round-trips per account and refuses out-of-range or corrupt values. Full 103-file suite (102 existing, unmodified, + this one) and `tsc --noEmit` both clean.
- **Still open**: `star_career_slots.sql` needs running in the Supabase SQL Editor before a second or third save follows a player across devices — until then, extra slots work normally on one device (both create and resume) but stay local to it, exactly as honestly degraded as this file's other pending-migration features.

**13 September 2026 (cont. 4) — Champions/Europa League rebuilt to the real 36 clubs apiece, England's other real qualifiers finally made visible, and a permanent main-nation fixed-count rule.**

- **The three-way inconsistency, found and root-caused.** Reported directly, with two pieces of real evidence: a claim that the simulation pools were 33/37 clubs (they were), and a live save showing 37 teams in the Europa League table. Real UEFA competitions are exactly 36 apiece. Investigating turned up a THIRD, independently wrong number: `clubs.ts`'s own `CHAMPIONS_LEAGUE_CLUBS`/`EUROPA_LEAGUE_CLUBS` (what the Lineups picker shows) had separately drifted to 38 clubs each. Three different systems, three different wrong numbers, none of them 36.
- **The real fix — one source of truth.** `clubs.ts`'s two lists were corrected to the real 36 apiece: FC København moved Champions → Europa League; Sevilla FC moved out to `OTHER_CLUBS` (Spain already has its full fixed allocation without it); Rangers FC, Hearts, and Vitória SC moved Europa League → `OTHER_CLUBS` (Scotland/Portugal already have real representation without them). `euro.ts`'s `seasonField` now builds the season's actual simulated field directly from these two corrected lists — not a second, separately hand-typed seed list (`CHAMPIONS_SEEDS`/`EUROPA_SEEDS`, both removed) that could drift from them again the way it reached 38 last time.
- **A much deeper bug, found while chasing the number**: only the player's OWN club ever actually represented England in the simulated Champions/Europa League field. Every other real English qualifier that season — the other 4 Champions League clubs, the other 2-3 Europa League clubs — was completely invisible from the competition, even though `competitions.ts`'s `seasonQualifiers` had always correctly computed who they were; that result was just never wired into `euro.ts`'s actual field. Fixed by extracting `qualificationFor`/`seasonQualifiers` into a new leaf module, **`lib/star/qualification.ts`** (avoiding a circular import: `competitions.ts` imports `openEuro`/`poolFor` FROM `euro.ts`, so `euro.ts` can't import `seasonQualifiers` back FROM `competitions.ts` — `competitions.ts` now just re-exports both functions from the new file, so every existing importer keeps working unchanged). Now every real English qualifier — not just you — genuinely appears as an opponent.
- **A new, permanent rule for the "big five" nations**, requested directly in full mechanical detail: England, Spain, Italy, Germany, and France each have a FIXED total club count per competition, forever equal to whatever their real season-1 rosters have (computed once from `clubs.ts`, never hardcoded — confirmed real values: England 5 Champions League/3 Europa League, Spain 5/2, Italy 4/3, Germany 5/2, France 4/2). England's specific occupants are real Premier League qualification (`seasonQualifiers`, above) — the one nation that's simulated rather than randomised, and its own worked caveat (a cup winner who didn't finish in the automatic places) is handled by topping up from the next-best-placed unclaimed English club when the cascade doesn't naturally reach the fixed Europa count. Spain/Italy/Germany/France instead draw a random SPECIFIC set of clubs each season from that nation's own real pool — its usual entrants plus its own "Other"-section reserves (the worked example given directly and verified: France always ends up with exactly 4 Champions League + 2 Europa League clubs, drawn from a real 8-club pool of PSG/Lyon/Lens/Lille/Marseille/Rennes/Monaco/Strasbourg).
- **The "everyone else" reshuffle** (every other nation, built in the previous session) now also draws from these same three relocated Other-section reserves (Rangers/Hearts/Vitória SC) as real, eligible entrants — split at least 1-and-1 across the two competitions for any nation with 2+ clubs, freely for a single-club nation — with genuine capacity clamping so a nation with more real depth than there are slots for it can have a club genuinely sit out a season, rather than being forced in wherever there happens to be room.
- **Four real implementation bugs caught and fixed during testing, not guessed** — the strict rule all session was to measure real output, never assume:
  1. **Capacity double-counting**: the first version of the "everyone else" split dumped every leftover flexible club into the Europa League regardless of its OWN real remaining capacity, silently overfilling it (measured: 36/28 instead of 36/36). Fixed by clamping each competition to its own separately-computed capacity.
  2. **`divisionOf`'s hidden overwrite bug**: `clubs.ts`'s shared `DIVISION_BY_CLUB` map is built by spreading the English-ladder lists first, then the Champions/Europa lists on top of the SAME map — so any club appearing on both (Arsenal, Aston Villa, Liverpool, Man City, Man United, and others) has its `"premier"` tag silently overwritten by `"champions"`/`"europa"`. This made every nation compute as "England." Deliberately NOT fixed at the source (used by ~19 other call sites, too risky this late) — worked around entirely inside `euro.ts` with a new `ENGLISH_LADDER` set built directly off `PREMIER_LEAGUE_CLUBS`/`CHAMPIONSHIP_CLUBS`/`PROMOTION_POOL_CLUBS`.
  3. **England's Europa allocation falling short**: `seasonQualifiers`'s Europa list is only ever 2 base slots plus an optional cup-cascade slot, so a season with no qualifying cup upset came up short of England's fixed 3. Fixed with a real top-up from the next-best-placed unclaimed English club in the actual table.
  4. **An odd-field pairing bug in `openEuro`** (caught by `euroTable.mts`'s own stress test, one call later this session): once the pool itself is a real, corrected 36, a career-less "you" (the dev sandbox, `/star-match-dev`, or any test club not already a real qualifier) being added ON TOP pushed the total to an ODD 37 — breaking `simulateEuroMatchday`'s assumption that an EVEN number of clubs remains once you and your opponent are set aside for a matchday. Measured directly: several clubs stuck on 7 played instead of 8. Fixed by trimming one real pool club to make room in that case, so the total field size is always exactly 36 regardless of which branch fires.
- Five test files needed genuine recalibration to the new real numbers, not weakened assertions: `euroReshuffle.mts` was fully rewritten (it tested the now-fully-replaced old 33/37 `reshuffleEurope` design); `euroTable.mts` and `competitions.mts` both had a stale `poolFor(...).length + 1` expectation baked in from when "you" was always guaranteed to be an addition on top rather than sometimes already a real qualifier — both now derive their expectation dynamically; `europeSquadNames.mts` updated for Copenhagen's real move to the Europa League; `saudiEurope.mts` dropped Rangers FC from its fixed Europa-League exemption list since Rangers is no longer a guaranteed fixture there.
- Full 108-file test suite and `tsc --noEmit` both clean.
- **Worth knowing**: the user's own recollection was that England had 6 Champions League clubs in season 1 — the real, corrected `CHAMPIONS_LEAGUE_CLUBS` list only has 5 (Arsenal, Aston Villa, Liverpool, Manchester City, Manchester United). The code derives the count dynamically from whatever the real list actually contains, exactly as asked, so this isn't a bug — just worth flagging since it doesn't match the number given verbally.

**13 September 2026 (cont. 5) — The English qualification rules corrected: only 6th is automatic, two independent cup slots, a real European Title Upgrade, and England's count is a floor, not a cap.**

- **A real bug found and fixed, caught by the user directly asking to re-derive the rules from scratch**: the previous pass (cont. 4, above) had BOTH 6th and 7th place automatically qualifying for the Europa League every season, no cup involvement needed. That's wrong — only 6th is an unconditional, guaranteed place. `lib/star/qualification.ts` was rewritten in full:
  1. Top 5 → Champions League, always.
  2. **Only 6th** → Europa League, always, separate from and on top of the two cup slots below (confirmed directly after ruling out a "6th loses its slot to a cup winner below it" idea the user brought in from an AI-generated reference document that turned out to be self-contradictory — discarded outright rather than implemented).
  3. Two independent Europa League slots tied to the FA Cup and League Cup winners: **Lock-Down** (the winner's own real table position) if they finished outside the top 6, or **Trickle-Down** (the next real position not already claimed by anyone) if they're already qualified through the league — never doubling anyone up, never skipping a slot.
  4. The **European Title Upgrade**: winning the Champions or Europa League itself (not a domestic cup) either upgrades an existing Europa League place to Champions League — with that Europa League slot **deleted outright, not passed down to whoever was next** — or, for a club with no European place at all, adds a genuine **pure bonus** Champions League place on top, expanding the real total. Never affects any other club's own slot.
- **England's count is a floor, not a cap** — confirmed directly, a deliberate exception to the otherwise-permanent main-nation counts: a pure-bonus qualifier can genuinely grow England's Champions/Europa League total past its normal 5/3. When it does, `euro.ts`'s `seasonField` doesn't grow the 36-club field or cap England back down — it shrinks the "everyone else" nations' own real capacity by exactly the excess, so a random NON-main-nation club (never German/English/Italian/French/Spanish) is the one that misses out that season, via the same capacity-clamping mechanism that already lets an over-subscribed nation sit out — not a second, separate displacement mechanism.
- New dedicated `seasonQualifiers` whole-table test in `competitions.mts` covers the full matrix against real named table positions: baseline (only 6th), both cup slots Locking-Down outside the top 6 (the user's own reported scenario — 15th and 17th both getting real places), both cup slots Trickling-Down from inside the top 5, a mixed case, a cup winner whose OWN slot already has the automatic 6th-place Europa place, the Upgrade's deletion (not cascade), the pure bonus, and the full worked "Maximum" example (7th/8th Lock-Down, 6th untouched, two separate pure-bonus qualifiers) — every check passed against the rewritten code on the first run, with no assertion adjusted afterward to force it. `europeanWinners.mts`'s stale 6th+7th-automatic assertions were corrected the same way. `euroReshuffle.mts` gained a new test proving a pure-bonus qualifier genuinely grows England's total while the whole field stays a real 36.
- Full 108-file test suite and `tsc --noEmit` both clean.

**13 September 2026 (cont. 3) — The Champions/Europa League field genuinely reshuffles every season from season 2 on.**

- **`reshuffleEurope`** (`euro.ts`) — requested directly, in full mechanical detail: from `career.season >= 2`, `poolFor` no longer returns the same static 33/37-club fields every year. A full real nationality map for all 70 clubs across both seed pools (`EURO_CLUB_NATION`, shared with the Saudi-clubs rule's own exemption check) drives the rule: England/Germany/Italy/Spain/France's clubs never move between competitions; every OTHER nation's clubs get reshuffled each season — freely if it has only one club in Europe, but with at least one club in EACH competition if it has two or more (exactly 1-1 if precisely two — proven against the user's own worked example, Scotland's three clubs, which never land all in one competition across any tested season). Season 1 stays the exact given rosters, untouched. Composes correctly with the Saudi-clubs rule when both are active — the Saudi swap applies on top of whatever the reshuffle already produced that season, not the original static pool.
- **A real capacity bug caught and fixed during testing, before it ever shipped**: the first version double-counted exempt clubs against the non-exempt capacity, silently shrinking the Champions League to 29 clubs and inflating the Europa League to 41 every season from 2 onward. Caught by a real sanity script measuring actual output, not assumed — fixed by tracking non-exempt allocation separately from the exempt count.
- `tests/star/euroReshuffle.mts` (new): season 1 is exactly the static roster; every season 2+ keeps the real field sizes, never moves an exempt club, always splits every multi-club nation across both competitions (1-1 exactly for a two-club nation), genuinely varies season to season, and is stable across repeated calls within the same season.
- Full 108-file test suite and `tsc --noEmit` both clean.

**13 September 2026 (cont. 2) — A real, votable Rule Book change: Saudi Pro League clubs in the Champions/Europa League.**

- **`saudiClubsInEurope`** (new `RuleBook` field, `ruleBook.ts`) — a real UEFA rule, proposed and passed through the exact same voting engine every other rule change already uses. Requested directly, in full mechanical detail: once it passes, two of the four real Saudi clubs (`clubs.ts`'s `OTHER_CLUBS` — Al Hilal, Al Nassr, Al Ahli SFC, Al Ittihad, which already have real squads fetched every season via `externalClubsFor`) join the Champions League, two join the Europa League — randomly, but never by bumping an exempt club. England/Spain/Italy/Germany/France's clubs are exempt from being replaced in EITHER competition (given directly); the Europa League additionally exempts five named clubs (Olympiacos, Anderlecht, Benfica, Rangers, Ajax). The two Champions League clubs that get replaced aren't dropped outright — they demote INTO the Europa League that same season, so the Europa League actually makes room for four incomers (two Saudi, two demoted), not two.
- `euro.ts`'s `applySaudiSwap`/`poolFor` (now takes an optional `career` — every existing caller without one behaves byte-identically to before): a real nationality map for the Champions/Europa seed pools decides exemption, the whole season's swap is computed once (seeded off `career.season`, stable across repeated calls within a season, genuinely different next season) so the Champions League's demotions land as real incomers in the Europa League field rather than a second independent random draw. Threaded through `openEuro` (`competitions.ts`), `entrantsFor` (`competitionBetting.ts`), and `strengthOf` (`investments.ts`) — the three real call sites.
- New Rule Book UI section (`RuleBookScreen.tsx`) under UEFA, alongside the other European-competition rules.
- `tests/star/saudiEurope.mts` (new): confirms the rule does nothing until a real vote passes, exactly two Saudi clubs land in each competition (never doubled up), every exempt club (by nationality and by name) is genuinely untouched, the two demoted Champions League clubs genuinely appear in the Europa League field, the swap is stable within a season and can reroll the next one.
- Also answered directly: yes, real-world club nationality is something I have real, reliable knowledge of across essentially this whole game's European roster — confirmed with several examples beyond the one asked about (KRC Genk/Belgium).
- Full 107-file test suite and `tsc --noEmit` both clean.

**13 September 2026 (cont.) — Real-world club prestige extended to the major European clubs.**

- `REAL_CLUB_PRESTIGE` (`investments.ts`) — asked directly to extend the researched prestige factor beyond the 20 English clubs. Real Madrid/FC Barcelona/FC Bayern München/Paris Saint-Germain now clamp to the same 3.0 ceiling as Manchester United (at this end of world football, precisely ranking the very biggest names against each other is genuinely not meaningful); Juventus/Atlético Madrid/Borussia Dortmund/Inter/AC Milan/Napoli sourced from real Forbes/CNBC 2025-26 global valuations. A second, explicitly lower-confidence tier (Roma, Ajax, SL Benfica, FC Porto, Sevilla FC, Galatasaray SK, Olympique Lyonnais) covers real, big, historic European names without pretending to a specific sourced figure for each. Still deliberately not exhaustive — most of the ~84 European/Other clubs this game knows about have no override and use the existing strength/tier-based estimate.
- Full test suite and `tsc --noEmit` both clean.

**13 September 2026 — CM unlock, three real Boardroom bugs, a real money stepper, training-facility-driven growth, and a researched stadium/facilities overhaul.**

- **CM position unlock.** Requested directly: a formation offering only ONE of the usual four alternates (ST/CAM/LW/RW — 4-3-2-1's "Christmas tree" and 5-3-2 both qualify, no wide men or almost none) now also offers Central Mid as a genuine second choice. `offeredPositions` (`teamsheet.ts`) — a property of the formation's own slot list, not a hardcoded formation id.
- **Highlight-timing delay fixed.** Reported directly: a keeper's save pushed wide, then 5-10 seconds before anything happened. Root cause: `canvasEngine.ts`'s dead-ball timeout only used its short 1.2s fuse once a real receiver had genuinely finished being involved — a direct free kick/training drill has no receiver AND no other runners at all, so it always waited out the full 3s timeout plus physics settle time regardless. Now any scenario with zero `orderableRunners` gets the short fuse too — nobody else was ever going to touch it.
- **Trial goal physics fixed.** Investigated by direct comparison against `CanvasMatch.tsx`: geometry and scoring logic were provably identical (shared constants/functions), but `TrialPenalty.tsx` called `stepBall` once per frame with the raw frame delta while real matches always subdivide into 3 fixed substeps — a real frame-integration-granularity bug explaining "the ball doesn't want to go in, like it's stuck." Fixed to match.
- **Buying your own club.** `canInvestIn` no longer blocks it. The Boardroom shows an honest financial-only view for your own club (squad/sign/manager tools stay with your real career systems — `findSquadEntry` only ever holds the OTHER clubs).
- **Club valuation rebuilt around real squad value.** `clubValuation` = (strength/tier curve × momentum + live squad market-value sum + club cash budget) × a real-world-researched prestige multiplier for ~20 actual English clubs (Man United/City/Liverpool highest, Burnley/Bournemouth lowest — real relative order, NOT real absolute money, compressed into a 0.6x-3.0x band so it doesn't dwarf this game's own economy). Buying a player for exactly his market value now leaves club value unchanged; a bargain raises it, overpaying lowers it — falls straight out of the real, live-tracked components rather than a second ledger. Extracted `tierOf`/`TIER_MULTIPLIER` into new `lib/star/clubTier.ts` to avoid a circular import with `marketValue.ts`. **Real regression caught and fixed**: summing a whole ~25-man squad at the original `marketValue.scale` (45) produced individual clubs worth millions — unaffordable even with a well-funded test career — so `marketValue.scale`/`marketValue.floor` were recalibrated down to 4/20 (from 45/200), landing a single very good player's price in the low thousands instead of the tens of thousands, consistent with the game's existing transfer economy.
- **Manager showing "Vacant" — fixed.** The Boardroom only ever read its own separate `ownedClubs[club].managerName` (a fictional Boardroom-hire record), never the REAL manager the admin actually assigns per club via the Lineups tool (`SavedLineup.manager`). Now reads the real one first.
- **Boardroom dropping back to Market after any vote — fixed.** Proposing a kit vote, a president vote, or a sell-player vote fully unmounts the Investments/Boardroom tree (the phase switches to "vote-ceremony"), wiping its internal tab/section state — coming back always reopened on Market instead of the club's own Boardroom. `investmentsEntry` now also carries a `section`, set right before each of these three vote-triggering actions, so the same club's Powers (or Squad, for a sale) tab reopens once the vote resolves.
- **Kit vote Design A/B becoming identical — a real, reproduced bug, fixed.** Reported directly: proposed a kit vote, Design B won, and the next time the kit picker opened, both designs showed the same colours. Root cause: Design B's picker always defaulted to the exact same hardcoded literal — the moment that literal ever WON a vote and became the current kit, Design A's default (which reads the real current kit) collided with it. Now picks a guaranteed-different second alternate. Also fixed the connected mislabeling bug: the ceremony always treated "Design A" as the "success" option regardless of which one the owner actually favoured, so a fair Design B win displayed as "the vote fails." Added real favour-A / favour-B / no-favourite vote buttons and threaded the real favour through to the ceremony's pass/fail framing.
- **A real money-input stepper.** Reported directly: negotiation/investment amount inputs' +/- arrows moved by ★1 per click — meaningless in the millions. New `niceMoneyStep` (`money.ts`) widens the step in real brackets as the amount grows (worked examples given directly all match exactly: ~★2M steps by ★250K, ~★50M by ★1M, ~★100M by ★5M) — wired into `NegotiationScreen.tsx`'s custom hold-to-repeat +/- buttons and `Investments.tsx`'s Buy control.
- **Training-ground tier now genuinely speeds up wonderkid growth.** Requested directly: a club's own real (if not literally-researched) training-facility level should matter, not just be a cosmetic upgrade number. `growWonderkids` (`leagueSquads.ts`) takes an optional per-club tier lookup (0.7x/1.0x/1.4x growth chance and amount for tier 1/2/3) — threaded from `careerFlow.ts` via `facilitiesFor`, avoiding a circular import (`leagueSquads.ts` sits underneath `investments.ts`, which `facilities.ts` itself imports).
- **A real, researched stadium-expansion overhaul.** Reported directly, with a worked example (10,000 seats on a 60,000-capacity stadium should take two seasons): real research (Charlotte 49ers' 2023 ~6,000-seat expansion, ~$9,300/seat; English top-flight new-builds far higher) grounds two real changes in `facilities.ts` — cost per seat now climbs steeply with EXISTING capacity (a real curve, not a flat rate), and paying for an expansion commissions it rather than applying it instantly: it queues as `stadiumBuild` and only actually adds the capacity once `progressStadiumBuilds` (wired into `advanceSeason`, alongside `creditStadiumRevenue`) counts the real season-length build time down — one season per 5,000 seats, matching the worked example exactly. Training-ground/youth-academy tier costs also steepened (top tier now several times the first upgrade, not the same again) — "easy to go from bad to mediocre, hard and expensive to go from great to world-class," requested directly.
- Full 106-file test suite and `tsc --noEmit` both clean.

**12 September 2026 (cont. 5) — One real money-formatting standard, everywhere.**

- **New `lib/star/money.ts` — `formatMoney`.** Requested directly, with exact worked examples: amounts of ★1,000+ now read with a k/m/b suffix, and every digit below the unit boundary becomes a real decimal digit rather than being rounded away — ★1,256 reads as "1.256k", ★12,500,072 as "12.500072m", an exact ★46,000,000,000 as a plain "46b" (no trailing ".000000000"), and ★12,500,000 as "12.5m" (trailing zero digits trimmed). Verified against every one of the user's own worked examples in `tests/star/money.mts` (new).
- **Replaced every duplicate, differently-rounded local `money()` helper** across `Investments.tsx`, `OwnershipScreen.tsx`, `NegotiationScreen.tsx`, and `DevMoneyPanel.tsx` (each used to independently `toFixed(1)`/`toFixed(2)` round to "1.3K"/"12.50M" — a real, if minor, inconsistency between screens) — all four now defer to the one shared implementation.
- **Fixed several balance displays that showed the RAW number with no formatting at all** — most notably `DashboardShell.tsx`'s own main balance strip, the very first money figure the game ever shows, alongside `HorseRacing.tsx`, `Shop.tsx`, and `Casino.tsx`'s bank displays.
- Full 101-file test suite and `tsc --noEmit` both clean.

**12 September 2026 (cont. 4) — Position-picker/market-value bugs fixed, league simulation and match ratings both made genuinely harsher/more realistic, and a dev "Add Money" tool.**

- **Position picker offering positions that don't exist in the real formation — fixed.** Reported directly: choosing Attacking Mid/LW/RW from the pre-match role picker did nothing because the club's real formation had no such slots — only Striker. Root cause: `PositionPicker.tsx` computed its alternates off `formationForClub(club)`, a generic hash-of-the-club's-name guess, instead of the REAL saved lineup formation `teamsheet.ts`'s own `build()` actually uses (`saved?.formation ?? formationForClub(club)`). Now reads the same real saved lineup first, so it only ever offers positions the actual team sheet can put you in.
- **Market value using the wrong club — fixed.** While confirming market values update live with rating/club changes (they do — the function is pure and recomputed fresh at negotiation time), found a real bug: signing a player from another club computed their market value using YOUR club's reputation tilt instead of the player's OWN current club's. Fixed in `Investments.tsx`'s `SignPlayerPanel`.
- **League simulation rewritten for real strength realism.** Reported directly, after extensive play: team rating barely seemed to matter in Star Career's league sim — bad teams doing well, good teams doing badly, big teams occasionally getting relegated — versus PL Draft mode's own simulation, praised directly as feeling like a real Premier League season off similar rating gaps. Root cause found and confirmed via direct comparison of both formulas: `simulateFixtureScore` (`lib/star/season.ts`) turned strength into a RATIO, compressing a realistic 25-point "contender vs relegation" gap into barely a 1.4x expected-goals swing, with a flat home-advantage bonus large enough to nearly cancel that gap for a strong side playing away. Rewritten to use the exact same LINEAR-DIFFERENCE shape Draft mode's own `computeExpectedGoals` already uses (`1.5 + diff × 0.065`, clamped 0.4-3.5) — the same size gap now produces roughly a 7-8x expected-goals spread, matching Draft mode's own praised behaviour. New "League Simulation" `tuning.ts` category for every constant.
- **Match ratings now genuinely punish a bad performance.** Reported directly, from a real deliberate test: kicking the ball off the pitch on purpose at literally every chance for a full 90 minutes still finished around a 6.4 — because `liveRating` (`lib/star/matchStats.ts`) never measured wasted chances at all, only goals/assists/passes/result. Fixed with a real penalty scaled to chances that produced neither a goal nor an assist (new `rating.wastePenaltyPerChance`/`rating.maxWastePenalty` in `tuning.ts`). Measured result: the exact reported scenario (6 wasted chances, a loss) now rates **3.85** — even a win the team achieved without your contribution rates **4.55** — versus the old ~6.4. Two existing tests (`selection.mts`, `week.mts`) had stale assertions baked to the old formula's specific numbers (one expecting an untouched 10→9.6 raw calculation, one expecting the old 5.7 floor) — both genuinely recalibrated to the new, intentionally harsher real behaviour with the reasoning documented inline, not weakened to force a pass.
- **New Settings → Dev "Add Money" tool** (`DevMoneyPanel.tsx`, new). Requested directly, for testing Investments/Boardroom (buying stakes, funding a club, negotiating transfers) without grinding for money first. Quick presets (+10K/+100K/+1M/+10M) plus a custom amount. Unguarded — same "developers and admins" spirit as the existing `DevSkipPanel` right above it on the same screen, which has never been gated either.
- Full 100-file test suite and `tsc --noEmit` both clean.

**12 September 2026 (cont. 3) — Real player market values and a full negotiation system, with an animated face-to-face UI.**

- **`lib/star/marketValue.ts` (new) — `playerMarketValue`.** Built exactly to the design proposed and approved: an exponential curve off overall rating (mirrors `clubValuation`'s own `Math.pow(strength - 40, 1.9)` shape — a few points at the top are worth far more than a few in the middle), a real age curve (a premium under 22, flat 22-28, a real decline past 28 that never reaches zero), a potential-tier multiplier reusing the existing `highPotential`/`worldClassPotential` flags (tapering off the older a tagged player already is — "the potential is closer to already realized or not"), and a small club-reputation tilt reusing `investments.ts`'s own `tierOf`/`TIER_MULTIPLIER` (now exported) rather than a second copy of "how prestigious is this club." Every constant lives in a new "Market Value" `tuning.ts` category. `tests/star/marketValue.mts` (new) checks the shape holds — climbs steeply with rating, rewards youth and potential, decays with age, never negative.
- **`lib/star/negotiation.ts` (new) — a real round-based negotiation engine.** Requested directly: buying/selling used to just hand over a fixed fee; now a seller always opens above `playerMarketValue` and a buyer always opens below it (real anchoring, never "fair" on the first offer), each round the counterpart concedes toward you scaled by a real mood score, and a stubborn lowball can genuinely sour that mood enough to make them walk away with nothing agreed — proven in `tests/star/negotiation.mts` (new) with a real measured result: haggling patiently ends up cheaper on average than caving to the opening ask. New "Negotiation" `tuning.ts` category for every anchor/concession/walkout constant.
- **`components/star/NegotiationScreen.tsx` (new) — the animated face-to-face UI.** Requested directly: two desks facing each other, and the counterpart's mood shown as an actual face rather than a number or word — happy 😊, neutral 😐, or visibly angry 😠, swapping live as the negotiation moves, with a scrolling log narrating each round's offer/counter. Wired into `Investments.tsx`'s Boardroom: signing a player from ANOTHER CLUB now opens a real negotiation first (free-agent signings skip it — there's no seller to negotiate with, the fee's always zero); selling a player negotiates a price with an interested buyer BEFORE the existing shareholder vote is even put up, so the vote is now over a real negotiated fee, not just a flat formula. `signPlayerForOwnedClub`, `sellPlayerFromOwnedClub`, and `proposeSellPlayerVote` all gained an optional `agreedFee` param that overrides the old flat `transferFee` when a negotiation supplied one; `resolveSellPlayerVote` now also reuses the exact fee that was actually voted on when completing the sale, rather than letting `sellPlayerFromOwnedClub` recompute its own fee fresh a second time.
- Full 100-file test suite and `tsc --noEmit` both clean. As with the rest of this session's changes, `/star-dev` requires Google sign-in, which this sandboxed environment can't complete, so the new UI hasn't been checked in a live browser.

**12 September 2026 (cont. 2) — Fixed 200%-ownership bug, shipped a consolidated "Ownership" home hub, a cinematic Vote Ceremony intro, and a market-value design proposal.**

- **A real bug: a club could be bought past 100% owned.** Reported directly, from a real save (200% owned). `buyStake` (`lib/star/investments.ts`) never capped `existing.percent + percent` at 100 — the UI's own spend cap (`Investments.tsx`'s `maxBuySpend`) only accounted for the bank balance, not what was already owned, so buying 100% and then buying more just kept adding. Fixed at both the real backstop (`buyStake` now clamps the purchase to whatever room is actually left, filling the remainder rather than rejecting outright) and the UI (the Buy control's spend cap now factors in the existing stake too, so it never dangles a bigger purchase than is possible). New regression test in `tests/star/investments.mts`.
- **A new consolidated "Ownership" home-page hub** (`components/star/OwnershipScreen.tsx`, new). Reported directly: Reputation, Rule Book, and Invest were three separate, unrelated-looking home buttons for what's really one system — "being kind of a chairman or a shareholder or a president." Replaced those three `QuickBtn`s on the dashboard with one "Ownership" entry (`app/star-dev/page.tsx`, new `"ownership"` `StarPhase`) that opens a single nicely-designed hub: a condensed reputation strip, governing-body influence/presidency badges, and a real card per club held a stake in (majority-owned ones link straight into that club's Boardroom). Deliberately does NOT reimplement Reputation/Rule Book/the Boardroom — every card taps through to the real existing screen, which now supports deep-linking (`Investments.tsx` gained optional `initialTab`/`initialBoardroomClub` props so a club card can open straight into its boardroom instead of making the player re-navigate tabs they just came from).
- **A cinematic two-stage intro for the vote ceremony.** Requested directly: "a desk... cameras pointed at you... you are proposing the changes... look at everyone in the crowd writing stuff down... then look above [and see] a board that shows the votes going up." `VoteCeremony.tsx` (the shared vote engine used by club/kit/Rule Book/presidency votes alike) now opens on a first-person podium stage (camera-flash icons either side, the proposal announced from your own desk) then a crowd-reaction stage (a small room of silhouettes visibly "writing it down," CSS-animated, no new image assets or canvas work) before moving into the existing vote-count/reveal exactly as before — the tally logic, options, and overrule flow are all untouched.
- **A player market-value design was written up and delivered to the user as a proposal, not implemented** (asked directly: "I don't want you to code this specific part... I want you to come up with an idea for how it should work"). Shape: an exponential base off overall rating (same idiom as `clubValuation`'s `Math.pow(strength - 40, 1.9)`), a real age curve (premium under 21, flat 21-28, declining past 28), a potential-tier multiplier reusing the existing `highPotential`/`worldClassPotential` flags, and a small club-reputation tilt reusing `clubValuation`'s own `TIER_MULTIPLIER`. Proposed as either replacing or sitting alongside `leagueTransfers.ts`'s existing `feeFor()`, with real transfer fees rolling randomness around the computed value rather than paying it exactly. Not built — a real next step if the user wants it.
- Full 98-file test suite and `tsc --noEmit` both clean.

**12 September 2026 (cont.) — Gray text replaced with bold white in Rule Book/Investments/Reputation/Vote Ceremony, and two real goal/assist distribution bugs fixed.**

- **Gray/faint text replaced with bold white** in `RuleBookScreen.tsx`, `Investments.tsx`, `SecondaryScreens.tsx`'s `ReputationScreen`, and `VoteCeremony.tsx` (the shared vote engine both use). Reported directly: low-opacity white body text (`text-white/40` through `/75`) reads as a wall of same-looking gray letters that's hard to be bothered reading. Descriptive/body text in these four areas is now solid white and bold; already-bold uppercase section-header labels were left as a slightly dimmer tier on purpose (still a real hierarchy, not more gray text) and intentionally-dimmed contrastive states (locked vs. unlocked achievements, disabled buttons, placeholders) were left alone since dimming those is a real signal, not neglect. This same low-opacity pattern exists in ~35 other screen files — not touched yet, flagged for a future pass if wanted, since doing all of them blind (no live browser this session) risks flattening some of those intentional dim/bright contrasts.
- **A real assist-concentration bug found and fixed.** Reported directly from a real month-long save: one teammate ended up with ~20 assists, a couple more with 7-10, and everyone else stuck at zero all season. Root cause in `squadData.ts`'s `pickSquadAssist`: strikers and centre-backs were hard-excluded from ever registering an assist at all, and the remaining ~5-7 eligible positions were drawn from UNIFORMLY at random, every week, all season — with a static saved lineup, that's the same small fixed pool the whole time, and plain variance over that many draws reliably produces a runaway leader and several permanent zeroes. Fixed to a weighted pool (mirroring `pickSquadScorer`'s own shape) that includes every outfield position — a striker's lay-off or a centre-back's knockdown can now register, just realistically rarer than a winger's or CAM's.
- **A real bench-vs-starter goal bug found and fixed, and the "are substitutes broken" question answered.** Investigated after a report that ~16 goalscorers in one real match week weren't in the lineups the user had built. Two different systems were involved: the user's OWN matches (and any match played live) have no bug at all — a benched player genuinely cannot register a goal or assist, because there's no in-match AI-teammate substitution system to begin with. The other 19 clubs' SIMULATED matches were the real bug — `leagueSquads.ts`'s `weightedPick` picked scorers/assisters from the whole ~20-man squad by position and rating only, with no concept of "starting XI" at all, so bench players (more numerous than starters at most positions) scored almost as often as starters. Fixed with a new `BENCH_WEIGHT` discount (0.22×) on any squad-array index past the starting XI's first 11 slots (the same starter/bench boundary `averageStartingXIRating` already uses) — bench goals still happen, just as the minority they are in a real match week, not the majority.
- Full 98-file test suite and `tsc --noEmit` both clean.

**12 September 2026 — Post-match Continue button relocated, three real media bugs fixed (rival-club leakage, England-tab richness, a manager re-hiring himself), and the casino's odds made realistic.**

- **Post-match "Continue" button moved off the bottom of the feed.** Reported directly: scrolling all the way down to leave Post Match Reactions after every match was tedious. `MediaFeed.tsx` now has two small Continue buttons instead of the old full-width one at the very bottom — a fixed one in the phone's own app bar (never scrolls away) and one as literally the first "post" in the scrollable feed (scrolls past exactly like any other post). The old bottom button is gone entirely, not just resized.
- **A real, provable "Club" tab leakage bug found and fixed.** Investigated via a background research agent after being reported directly ("it shows three messages about a Leeds game... Aston Villa and Nottingham Forest... nothing to do with my club"). Root cause: `select.ts`'s `allegiance()` scored a rival-fan account 0.2 — a real, non-trivial score, not near-zero — for literally every OTHER club's match, not just ones involving your club or the rival's own club, backwards from its own stated comment. That let a rival-fan account post about a totally unrelated third-party fixture. Fixed to score 0 for anything that isn't actually about your club.
- **A real England-tab richness gap found and fixed.** `lib/star/media/detect/league.ts` only ever produced flat win/draw/loss/rout/hammered scorelines and hat-tricks (3+) for other clubs' matches — a 2-goal game (the user's own example: Cole Palmer's brace in a 3-2 Chelsea win over Man City) was silently dropped, and there was no equivalent of the player's-own-match "winner"/"late-winner"/"equaliser" detectors naming the actual decisive goal. Added a brace detector (n===2, reusing the "brace" id/importance `detect/goals.ts`'s own HAUL already uses) and a genuine decisive-goal reconstruction (`decisiveGoalEvents`/`decisiveDrawEvent`) that walks each match's per-goal minutes to name the real match-winner or, in a draw, the real equaliser — same technique `detect/kit.ts`'s `isWinner`/`isEqualiser` already use for your own match, just rebuilt here since `LeagueResult.hg`/`.ag` only carry a minute per goal, no running score. No new templates needed — the existing club/press/data templates for `winner`/`late-winner`/`equaliser`/`brace` are already club-name-generic.
- **A real, reproducible false "manager sacked" bug found and fixed.** Reported directly: a post said "Iraola leaves Liverpool..." but Settings still showed Iraola as the manager. Root cause in `careerFlow.ts`'s `advanceSeason`: when a real sacking fires, the outgoing manager was added back into the SAME candidate pool used to immediately roll his replacement in the same call — so he could occasionally get re-rolled as his own replacement, firing a genuine sack narrative while `career.manager` never actually changed. Fixed to roll the replacement from the pool WITHOUT him first, then add him back only for a later vacancy.
- Also carried over from the previous session (now committed together): horse-racing odds and competition-betting odds made realistic — `Casino.tsx`'s horse race now runs a real live Monte-Carlo simulation off each horse's rating (instead of a rigged/fixed outcome) so quoted odds always match the actual race, and `competitionBetting.ts`'s book uses a steeper strength-to-price exponent plus a much higher realistic max price (`betting.*` in `tuning.ts`, all now tunable) — reported directly with real numbers (a title favourite priced at ~10/1 when the real equivalent is 2-4/1; a hopeless underdog capped near 35/1 when Leicester's real 5000/1 title win is the benchmark for how long odds can genuinely run).
- Full 98-file test suite and `tsc --noEmit` both clean. As with the rest of this session's changes, `/star-dev` requires Google sign-in, which this sandboxed environment can't complete, so none of this has been checked in a live browser — confidence rests entirely on the type-check and test suite.

**11 September 2026 (cont.) — The REAL club-badge circle finally found (a fourth attempt), every real team-sheet squad restored, and competition betting given a real cutoff.**

- **Team sheets showing fake/silhouette players for every club — root cause found and fixed.** The wonderkids feature added `high_potential` to `app/api/star/league-squads/route.ts`'s `.select(...)` — but the corresponding migration (`high_potential.sql`, still PENDING) was never run against the live database, and Supabase fails the WHOLE query when a selected column doesn't exist (the same behaviour the admin page's own "Critical Gotchas" note already warns about). That meant EVERY real squad fetch for EVERY club failed the instant that column was added, silently falling every team sheet back to `generatedSquad`'s fake roster with silhouette portraits — reported directly: "both teams all have silhouettes... made up players... for every single club." Fixed at the route: it now retries the query once without `high_potential` if the first attempt errors, so a pending migration degrades to "no wonderkid flag yet" instead of "no real players anywhere." Wonderkids themselves still need the migration run to actually do anything.
- **The real club-badge circle, actually found — a fourth pass.** Reported four times now. Two earlier fixes chased a decorative header glow (never the cause) and a `bg-white/10` fill showing through a crest's transparent pixels (real, but only half the bug). This pass found BOTH remaining causes and removed them outright rather than reshaping again: `ClubBadge.tsx`'s real-crest `<img>` still had `rounded-full` on it, clipping a real crest (almost never circular — Forest's tree, United's shield) into a circle regardless of any fill; and `VersusScreen.tsx` had its own separate, independently-added ring wrapper around the shared `Crest` component (`rounded-full border-2 ... bg-black/20`) specifically for the team-sheet header — the same class of bug (a translucent circular fill showing through the crest's own transparent pixels) reintroduced one level up after the first fix. Both deleted, not reshaped.
- **Competition betting given a real cutoff.** Reported directly: with no cutoff, a bet placed on the last day of the season (once a title was already effectively decided) was a free win, not a real bet. `lib/star/competitionBetting.ts`'s new `canPlaceCompetitionBet` closes betting the moment the summer transfer window shuts (before August, per `calendarMonthOf`) — the same real calendar boundary `careerFlow.ts`'s own deadline-day event already fires on, read directly off the live calendar (not `career.lastTransferWindowKey`) so season 1 — whose own summer window is deliberately never run — still gets a real, working cutoff rather than a special case. Enforced both in the Casino UI (`Casino.tsx`, disabled + a clear banner once closed) and at the actual placement handler (`page.tsx`) for defense in depth.
- New `tests/star/competitionBetting.mts` section for the cutoff; full test suite and `tsc --noEmit` both pass.

**11 September 2026 — The real white-circle culprit found, wing-backs pushed forward, and the first-person duel mode now replaces the real dribble scenario.**

- **A real 21-club Premier League bug, self-healed.** Reported directly from
  a real save at season 3: the league table held 21 clubs instead of 20.
  Ruled out the current ladder math as the cause — `tests/star/promotion.mts`'s
  existing 20-season `advanceSeason` stress test passes cleanly, and there's
  no duplicate club name anywhere in `lib/star/clubs.ts` — so the corruption
  in that specific save is almost certainly a relic of an already-fixed
  historical bug, permanently baked into that save's JSON. Rather than chase
  a cause that isn't reachable from today's code, added a defensive
  self-healing step: `reconcileLadder()` (`lib/star/promotion.ts`), run at
  the end of every `resolveLadder()` call, de-duplicates any club sitting in
  two tiers at once and resizes each tier back to its correct count (excess
  demoted weakest-first, shortfalls promoted strongest-first from the tier
  below) — never inventing a club or silently dropping one. A save carrying
  this corruption self-corrects at its very next season rollover with no
  action needed. New regression test in `tests/star/promotion.mts` feeds
  `resolveLadder` a deliberately-corrupted 21-club input and asserts it heals
  to exactly 20/24/5; full 90-file suite + `tsc --noEmit` both clean.
- **Star Power & Politics — Phase 0 resolved, then Phases 1 through 4
  actually built, all in one session.** The huge future club-ownership/
  governance feature (`STAR_POWER_POLITICS.md`, `[[star_power_politics_project]]`
  memory) had four open design questions blocking Phase 1. All four
  settled first, no code touched: the Champions League format target
  (rule #7) is groups-of-4-then-knockout; the host-a-final home-advantage
  effect (rule #9) applies only when your own club is one of the
  finalists; the governing-body map ships with just FA/UEFA/FIFA/CONMEBOL;
  and §4.5's aging-up "son" mechanic is cleared to build — confirmed
  fictional (a potion/magical-ageing effect on a game character, not
  real-child doping) and scoped into Phase 3. Then, same session:
  - **Phase 1** — `lib/star/reputation.ts`, a real `Reputation` field on
    `CareerState` (world/club/government/shareholders; fan reputation
    stays `relationships.fans`, not duplicated), hooked into
    `advanceSeason` (trophies nudge world reputation, the season's board
    judgement nudges club reputation), new `ReputationScreen`.
  - **Phase 2** — `lib/star/voting.ts`, the generic vote/ceremony engine
    named in §3 (real per-option counts over a real electorate,
    reputation-biased but never guaranteed, an overrule gated above 75%
    ownership that always costs shareholder reputation), proven end to
    end on selling a player from an owned club (`investments.ts`'s
    `proposeSellPlayerVote`/`resolveSellPlayerVote`) via a new, fully
    generic `VoteCeremony` component. A real bug was caught and fixed
    during testing: `castVote`'s no-favoured-option case was silently
    defaulting roughly half of the unclaimed probability to whichever
    option was listed last, so a "neutral" vote actually ran ~75/25.
  - **Phase 3** — new `lib/star/clubPowers.ts`: minority-shareholder
    recommendations (a real, reputation-biased "the board considers it"
    roll), formation-as-manager (`clubStrengthWithFormation` — a small,
    bounded ±3 real strength effect reusing `formations.ts`'s own
    `autoPick`/`bestFitness`, never rewriting the stored league strength),
    a kit creator + a real fan vote, a shareholder-elected presidency +
    a wage paid from the club's own budget, the §4.5 son mechanic (real,
    capped, random age/ability jumps via "the potion," free transfers
    because "you're his dad"), and club mergers (100%-ownership-of-BOTH
    required, the survivor's squad capped at the normal squad-size limit,
    the absorbed club's real squad genuinely emptied, a real fan-
    reputation cost — deliberately does NOT remove the absorbed club from
    `clubs.ts`'s fixed lists, since that would break the ladder's fixed
    division-size invariants `promotion.ts`'s `reconcileLadder` exists to
    defend). A minimal but real "Powers" tab in the Boardroom screen, and
    a "Recommend" action on minority Portfolio stakes.
  - **Phase 4** — new `lib/star/governingBodies.ts` (§4.2's map as real,
    flat-priced 0-100 influence data) and `lib/star/ruleBook.ts` (points
    per result, no-draws-go-to-penalties, match length — reusing Phase
    2's `castVote`/`VoteCeremony` exactly, gated by influence, costing
    WORLD reputation on overrule rather than shareholder). `season.ts`'s
    `playLeagueWeek`/`updateLeagueWithUserResult` both take an optional
    rule-book parameter defaulting to classic 3/1/0-with-draws, so every
    existing call site sees byte-identical behaviour unless it opts in —
    only `careerFlow.ts`'s two real call sites now pass the FA's active
    rule book. Match length threads into `CanvasMatch.tsx` as a local
    value, changing when the match ends and how energy drains —
    deliberately NOT retuning `matchLog.ts`'s own commentary pacing
    (calibrated for 90 minutes), the honest, contained slice of what the
    rollout plan itself flagged as the heaviest lift of the three. New
    `RuleBookScreen`. Only the FA's rule book reaches anything this
    career actually plays — UEFA/FIFA/CONMEBOL are real, investable, but
    nothing they govern is simulated deeply enough for these three rules
    to show an effect yet.
  - `tests/star/reputation.mts`, `voting.mts`, `clubPowers.mts`,
    `ruleBook.mts` (all new) plus `investments.mts`'s own new voting
    section all pass.
  - **Phase 5** — new `lib/star/corruption.ts`: bribery (`bribeVote`, sways
    real votes toward a target, capped at 15% of the electorate — never a
    manufactured landslide), hiring lawyers (genuinely cuts exposure risk,
    never removes it — measured across 300 trials), and the black market
    (`RuleBook.bannedItems`, a real markup, wired into the real Shop
    screen with a BANNED tag) — all one shared `applyGettingCaught`
    consequence (a capped fine, a world-reputation hit, damaged boss/fan
    relationships, and a genuine suspension reusing `career.injury`'s
    exact shape rather than a second mechanic).
  - **Phase 6** — the offside toggle is genuinely wired (a module-level
    switch in `canvasEngine.ts`'s `offsideSnapshot`, set per match from
    the Rule Book, proven against a real rigged offside-position
    scenario); European slot reallocation is genuinely wired (real
    optional params on `competitions.ts`'s real `qualificationFor`/
    `seasonQualifiers`); forced league movement is genuinely wired via new
    `lib/star/forcedMovement.ts`, extending `promotion.ts`'s
    `reconcileLadder` with a real new "limbo" tier that returns to next
    season's real promotion pool — deliberately never displaces the
    player's own club; new competition creation is a real, standalone
    knockout bracket (`lib/star/newCompetition.ts`), deliberately not
    coupled to the existing FA Cup/League Cup machinery. Squad size and
    the Champions League format ship as real, VOTABLE data with an
    HONESTLY STATED gap — no gameplay hook for either yet, given the
    size/risk of rewriting the match engine or `euro.ts`'s Champions
    League simulation safely alongside five other phases in one sitting.
  - `tests/star/corruption.mts`, `phase6.mts` (both new) pass.
  - **Phase 7** (the last phase in the plan, confirmed a genuine quick
    win) — new `lib/star/facilities.ts`: every club gets a real,
    deterministic stadium/training-ground/youth-academy (hashed from the
    club's own name along three independent axes, never re-rolled),
    majority owners can rename/upgrade each for a real cost from the
    club's own budget, and a bigger stadium earns real gate-receipt
    revenue into the club's own budget every season
    (`creditStadiumRevenue`, wired into `advanceSeason`) — never touches
    the player's personal money. New Facilities section in the
    Boardroom's Powers tab. `tests/star/facilities.mts` (new) passes.
  - **All seven phases of the original rollout plan complete — then an
    unplanned Phase 8, same session.** Asked directly to also build
    "president/king of a real country" (§5's end-game framing, previously
    cut for having no concrete mechanic) and to wire real gameplay hooks
    for squad size/Champions League format. New `lib/star/leadership.ts`:
    president of a real governing body, not an invented country (nothing
    here models a country as its own entity, and a governing body already
    is one) — real thresholds on two axes (90+ world reputation AND 90+
    influence in that body), a real vote reusing Phase 2's engine, an
    overrule bar genuinely higher than the standing bar. The actual
    point: a president genuinely bypasses the ordinary influence gate on
    three real powers (proposing a Rule Book change, overruling a vote,
    forcing a club's league position) — each of those gates now checks
    the presidency as an alternative path. New Presidency section in the
    Rule Book screen. `tests/star/leadership.mts` (new) passes.
  - **Squad size and Champions League format deliberately stayed
    data-only** — considered, not skipped. The only safe partial hook for
    squad size (roster/bench size) would misrepresent what the rule
    actually means (on-pitch numbers, needing a genuine rewrite of
    `formations.ts`'s fixed-11 assumption and every team-sheet screen);
    the Champions League rewrite needs `euro.ts` (731 lines, tightly keyed
    to a single league-table phase) to grow a real parallel groups-of-4
    simulation. Both stay honest, votable data with the gap stated
    plainly in-game, rather than forcing something fake or risking the
    tested match/competition engines — real, concretely scoped next steps
    for a dedicated future session.
  - Full 98-file suite and `tsc --noEmit` both clean. Live browser
    verification of any of these eight phases' new screens wasn't
    possible this session — `/star-dev` requires Google sign-in, which
    this sandboxed session can't complete — so all of this rests on the
    type-check and full test suite only; worth a real playthrough check
    next time someone's signed in.
- **The team-sheet white circle/oval, actually found this time.** Reported three times; the first two "fixes" both chased `VersusScreen.tsx`'s header glow, which was never the cause. The real source: `ClubBadge.tsx`'s real-crest `<img>` had `bg-white/10 rounded-full` on it — a real crest (Forest's tree, United's shield) is almost never a perfect circle, so that fill showed straight through every transparent pixel the crest itself doesn't cover, different-shaped per crest (a circle for one club, an oval for another) and shifting slightly at different zoom levels along the crest's own alpha edge. Reproduced the exact mechanism with a standalone shield-SVG test page (before/after) to confirm before touching the real code. Fill removed entirely.
- **Wing-backs pushed forward.** Every 5-at-the-back formation (`back5()`, `lib/star/formations.ts`) had all five defenders on one flat `WB` line. Added `WB_ADV` (a touch ahead of `WB`) for just the LWB/RWB slots — reported directly, with a marked-up screenshot circling exactly where they should sit.
- **The real dribble scenario replaced with the first-person duel mode** — the biggest change this session. Requested directly, explicit values given:
  - **The old system is fully preserved, not deleted.** `lib/star/dribble.ts`, `CanvasMatch.tsx`'s `dribbleRef`/`finishDribble`/the `"dribble"` phase's own draw()/pointer-handling code are all still there, untouched, and still work. `CanvasMatch.tsx`'s new `USE_FIRST_PERSON_DRIBBLE` constant (currently `true`) is the ENTIRE difference — flip it to `false` and real matches use the old top-down flick-to-run scenario again, nothing else to restore. The old system, for reference: a top-down run through midfield (`newDribble`/`stepDribble`), 3-4 stationary chasers that wake at 7m and tackle at 0.9m, one flick picks your whole heading, "through" chains into a generic scenario built from wherever you ended up (usually mid-30s metres out — NOT near the box), "lost"/"out" is a turnover.
  - **New system**: `components/star/FirstPersonDribble.tsx` (built and tuned across earlier sessions in `/star-dribble-dev`) now mounts as a full overlay over `CanvasMatch`'s own canvas when `phase === "fpDribble"` (new `embedded` prop skips its standalone full-page wrapper/heading and its "Go Again" button — the match itself decides what happens next, same beat the old system used). Production values: `pace=100, oppStrength=100, chaseEye=5, chasePitchDeg=5, chaseOffset=4, cameraFollowRate=10`, `ballTouchReach` left at its own default (0.9 — "whatever it started as in the testing area").
  - **Wave shape, capped at ten total defenders**: new `pickWaveSizes()` (`lib/star/firstPersonDribble.ts`) rolls 2-4 waves of 1-4 men each, but the total across the whole run never exceeds 10 — requested directly, with the reason given: "in a real match there's only eleven players, and one of them is a goalkeeper." The cap is a hard ceiling, not a target — a wave shrinks or the run ends up with fewer waves than planned before it's ever exceeded. Threaded into `newRun()` via a new optional `waveSizes` param that takes priority over the old `rounds` count and skips the internal random-per-wave roll (the dev sandbox and existing tests don't pass it, so they're unaffected).
  - **Beat 7+ defenders and clear the run → a real attacking scenario near the box.** `FirstPersonDribble`'s `onComplete` now reports `beaten` (every defender individually marked `"beaten"`, not a re-derived guess) alongside `cleared`. `CanvasMatch.tsx`'s new `finishFpDribble`: beating fewer than 7 and clearing chains into the same advanced-midfield position the old system always used (~30m out, ambition 1 — an ordinary follow-up chance); beating 7 or more chains into a position explicitly inside the box, near the penalty spot (`PEN_SPOT_Y`) — which lands in `chainKindFor`'s existing close-range branch (~45% `one_on_one`, else `volley`/`tight_angle` — real shooting-shape chances, no through-ball/cross routing), reusing the existing, tested scenario machinery rather than building a bespoke minigame (confirmed via investigation: no such bespoke system exists anywhere in this codebase — every chance, however it's created, resolves through the same generic aim/contact/flight machinery).
  - `tests/star/firstPersonDribble.mts` extended: `pickWaveSizes`'s wave-count/per-wave/total-cap bounds (including a tight-cap case proving the cap actually wins over the ranges, not just coincidentally never binding), and `newRun`'s `waveSizes` override being used verbatim.

**10 September 2026 — England tab (real league-wide media), a real TrialPenalty bug, club logos investigated twice, and why Adobe/Supabase/Vercel don't work in this session.**

Several distinct threads, worked in the order reported.

- **Casino odds → decimal format** (`Casino.tsx`, `competitionBetting.ts`) — `{odds.toFixed(1)}:1` → `{odds.toFixed(2)}`, at every site odds are shown.
- **Investments sign/sell bug fixed**: `setSquad()`/`signPlayerForOwnedClub`/`sellPlayerFromOwnedClub` (`lib/star/investments.ts`) only ever looked in `career.leagueSquads`, which holds only the OTHER clubs in your CURRENT division — a club you own outside it (bought via investment, not promotion) lives in `career.externalSquads`, the same two-array split `scoutReport.ts`/`teamsheet.ts` already handle correctly. Fixed with a shared `findSquadEntry()` helper; `Investments.tsx` also stopped swallowing failure reasons (buttons did nothing with no explanation — now surfaces `result.reason` in a banner).
- **First-person dribble mode** (`/star-dribble-dev`) — several rounds: (1) ball occlusion fixed — only the shins now draw after the ball (torso/arms never did; full-figure occlusion was tried and measured to hide the ball almost the entire run) and boot rendering bugs (backwards toe/heel offsets, detached lift) fixed via a real Playwright render harness, not guesswork; (2) wave redesign from 3 fixed one-at-a-time defenders to 1-3 random men per wave placed across the corridor (`placeWave`, `lib/star/firstPersonDribble.ts`) — caught two real fairness bugs by measuring an oracle against the sim rather than trusting the design: one-per-band placement guaranteed a pincer (~6% clear regardless of skill), and a stale "burst away from commitSide" oracle rule stopped being correct once defenders no longer always fully converge onto your lane; (3) reported "you don't even have to time it" — measured and confirmed: the assist glow (default on) revealed the answer outright, and a burst fired the instant a man committed (his lunge by then a fixed non-reactive function of time) still cleared 32.3% of the time. Fixed by defaulting the assist glow off and having `applyBurst` fix the burst's power at fire-time (full only while genuinely telegraphing, 0.3× once committed) — same measurement afterward: 2.9%. (4) Wave size widened again, 1-3 → 1-4 men, per direct request — real difficulty curve measured and the "reading it right" oracle's threshold in `tests/star/firstPersonDribble.mts` updated to the real number (17.7%, down from 34.4%), not left stale. See `tests/star/README.md`'s `firstPersonDribble.mts` section for the full numbers. **Still open, reported directly and not yet fixed**: the ball still reads as behind/attached to the player's body rather than clearly in front from the chase-cam, and the leg/reach motion still looks unnatural ("flailing") — a render-harness investigation was mid-flight (found the likely cause: at this camera's geometry a ball only ~1m ahead of the feet already projects up near hip/chest height on screen, which several camera-constant tweaks didn't fix enough) when the conversation moved on to other reports. Picking this back up needs the harness approach (`renderFirstPerson`'s `own` path, rendered via a transpiled-JS + Playwright screenshot loop — real numbers, not description) rather than more constant-guessing.
- **Team-sheet screen (`VersusScreen.tsx`) redesigned, then partly reverted**: first pass added a bordered competition badge + ball icon, ringed crests, a bigger "VS", a calendar icon, floodlight glow in the header/pitch corners, and corner arcs/D-arcs on the pitch. Reported directly as not matching the given reference image — the glow read as unwanted "blur" and the arcs as "irregular circles". Pulled the glow and the arcs/D-arcs back out; kept the badge/crest-ring/VS/calendar changes, which more literally matched what was asked for.
- **A real England tab — league-wide news, not a relabel**: replaced the old All/News/Stats/Fans phone tabs (all four filtered the SAME club-only pool) with All/England/Club. `lib/star/media/detect/league.ts` (new) turns every OTHER club's fixture that week into the same win/draw/loss/rout/hammered and hat-trick events `detect/result.ts`/`detect/goals.ts` already produce for your own match, reusing `playLeagueWeek`'s (`season.ts`) already-simulated named scorers — that data existed and simply never reached the media engine before. The existing "club"/"league" archetype templates turned out to already be club-name-generic, so no new templates were needed — only a real bug found and fixed in `select.ts`'s `allegiance()`: every event until now was always about `yourClub`, so keying "is this my club" off `subject.kind` happened to work without ever checking WHICH club; rewritten to key off `facts.club` so a third club's own account can speak for its own result while your fans/club stay out of news that isn't theirs. New `StoredPost.scope` ("club"/"league") drives the tabs; `MediaState.lastLeagueCycleId` is its own independent replay guard so it can never collide with the match/career cycle guard the post-match "moment" screen depends on. `tests/star/leagueMedia.mts` (new). Investigated the ALSO-reported "I never see the chants" complaint first: the mechanic is fully wired and fires correctly — it's tagged only `"goal"`, so under the OLD tabs it only ever showed under All or Fans, easy to miss.
- **Club logos — investigated twice, still not confirmed fixed live.** First pass: `lib/star/clubLogos.ts`'s `normalizeClubKey` only survived cosmetic drift (accents, "&"), not an actual SHORTER name — "Man United" (this game's canonical name) and "Manchester United" normalise to two different strings, so a dataset using either the game's own names or common short forms (Man Utd, Brighton, Spurs, Wolves…) could miss every match. Added `CLUB_LOGO_ALIASES` + `lookupClubLogo()` fallback (tries the club's own key, then any alias pointing at it), wired into `ClubBadge.tsx`; also added real crests to `LeagueScreen.tsx`'s table rows (previously plain text, no badge at all). **Reported directly afterward that logos still don't show** — every badge still falls back to the plain kit-and-initials circle. Root cause not yet found: no live Supabase access this session to actually inspect what's really stored in `club_logos` or whether the anon/browser read is even succeeding. `getClubLogoMap()` was silently discarding any Supabase `error` (destructured `{ data }` only) — now logs it (`console.error`) rather than treating a failed read identically to an empty table, so the NEXT session with a real browser/console can actually tell the two apart. Whoever picks this up next: check the browser console for that log first, then check what's actually in `club_logos` (Table Editor) against the star game's real club names (`lib/star/clubs.ts`'s `PL_CLUBS`/etc.) and its RLS (`supabase/migrations/club_league_logos.sql` says public-read, but that migration's actual run status against the live project was never confirmed this session).
- **TrialPenalty (the career's opening penalty) diverged from CanvasMatch's real in-match penalties** — reported directly, with specifics (harder to score, keeps hitting the post, "the ball just gets stuck on the post"). Found two real, provable bugs, not vibes: `MIN_PULL`/`FULL_POWER_PULL` were stale copies (0.04/0.16 flat) of constants the real match itself moved on from (0.008, and a skill-scaled `dragForFullPower()` topping out at 0.14) — the trial's own comment even still claimed a parity it no longer had. And once ANY outcome fired, the trial simply froze the ball in place for its whole 1.1s settle beat, never calling `settleBall`/`stepBallPastBar` the way CanvasMatch does — so a post-out or an over-the-bar miss visually stopped dead exactly where the outcome was decided instead of being seen rolling/flying away. Both ported into `TrialPenalty.tsx`.
- **Adobe (and every other remote connector) confirmed not usable from THIS session type** — checked properly rather than guessed: `ListConnectors` shows Adobe for Creativity `connected: true`/`enabledInChat: true` at the account level (not an auth/reconnect problem), and every actual tool call fails identically with `SdkHttpError ... CLIENT_HTTP_NOT_IMPLEMENTED` dialing Anthropic's own MCP bridge — and this is NOT Adobe-specific: Supabase, Vercel, PostHog, Sentry and even `Claude_Code_Remote` all fail the exact same way in this session. The local network proxy (`$HTTPS_PROXY/__agentproxy/status`) is healthy with no relay failures, but doesn't even apply — `api.anthropic.com` is explicitly routed around it. This points to a limitation of THIS session type (a remote/cloud "Claude Code on the web" container) rather than anything wrong with the connectors, the project, or the user's account: a locally-run Claude Code (desktop app or CLI, on the user's own machine, as a genuinely NEW session rather than one resuming/pointing at this remote one) is the likely way to get working connectors, though that's an inference from the pattern, not independently verified from inside this sandbox. **Don't waste a future session re-trying these connectors from inside a remote/cloud session** — check the session type first.

**31 August 2026 — Energy rebuilt for real, and a genuinely new injury system.**

Built to the exact three-point brief that had been sitting in this file's Future Work section: a minimum energy to START a match, a minimum energy to be trusted as a SUBSTITUTE, and energy that regenerates only when you deliberately skip the rest of the week — never automatically, which is what made the old version boring enough to remove in the first place. Rebuilt against the real git history of what energy used to touch (`WEEK_RECOVERY`, `REST_ENERGY`, `hookCheck`'s old `"legs"` formula, `hiddenMatch`'s folded-in involvement weight) rather than guessed from scratch.

- **`CareerState.energy`** (0-100): drained on `creditMatchResult` proportional to minutes played, guarded against a replayed fixture double-draining it; reset to 100 at season rollover. Earned back only two ways — Rest (now also tops up energy, not just happiness) and a new **"Skip to Match Day"** action in the Life tab that forfeits the rest of the week's actions for a bigger top-up.
- **Two hard gates in `selectionFor()`** (`lib/star/selection.ts`), applied on top of its existing standing-based verdict: below `MIN_ENERGY_TO_START` a 1st-team standing is capped at Substitute; below `MIN_ENERGY_TO_SUB` even a bench place is withdrawn to Squad. No new substitution UI needed — "coming on as a sub" was already fully represented by `selection.onAt` feeding `CanvasMatch`'s `startMinute` prop.
- **Live in-match fatigue**, transient and separate from the persisted value: `CanvasMatch.tsx`'s new `liveEnergyAt(minute)` eases down over the ninety simulated minutes and feeds a reinstated `tiredSkills()` (power/technique shaved modestly), `hookCheck`'s reinstated `"legs"` `HookReason` (tired-legs substitution risk), and `hiddenMatch.ts`'s involvement formula (effort buys involvement, not better football) — all optional so the star-match-dev fork and any caller that doesn't track it behaves exactly as before.
- **Injuries — new, nothing like it existed before** (`leagueTransfers.ts` used to say so explicitly). `CareerState.injury`, rolled once per match played, risk climbing sharply as end-of-match fatigue drops (rare but real even fully fresh — freak knocks happen). `selectionFor()` returns `"Injured"` unconditionally while carrying one, overriding even a career-best standing; the pre-match screen swaps Play Match for Watch from the stands with a banner naming the injury and weeks remaining. Weeks tick down on every week that passes without playing, clearing at zero and at season rollover. Deliberately scoped to the player character only — no other squad player has any per-player state tracked anywhere in this game beyond goals/assists, so teammate injuries are a separate project.
- UI: an Energy row (Life tab, matching the relationship-bar style) beside the new Skip button; an Energy tile on the dashboard's Status tab and the pre-match screen (mirroring Match Fitness); a fatigue warning and the injury banner on the pre-match screen, reusing the existing "boots need replacing" warning-line pattern.
- Old saves backfilled in `storage.ts` (same place `manager`/`squadNumber`/`squad` already are) so a save from before either field existed opens at full energy with nothing wrong, not a NaN-corrupting `undefined`.
- `tests/star/energy.mts` (new) covers the drain/regen arithmetic and the injury roll's probability curve and duration split; `week.mts`/`selection.mts`/`hiddenMatch.mts` extended for Rest/Skip regen, the `"legs"` hook reason, the two selection gates, the Injured override, and the reintroduced involvement term. One pre-existing test needed its setup adjusted (five full matches back to back with no recovery, which energy now correctly punishes on its own) rather than the assertion weakened.

**30 August 2026 — Full match-feedback pass, real terrace chants, a false-award bug fixed, and energy pulled from the UI pending a real implementation.**

One long working session, several batches.

- Post-match feedback batch (a full itemized list from actually playing a match): scoreboard uses each club's short name when it overflows; the live commentary ticker/hint row and the aim/contact-screen tutorial diagram+text now only show in the trial, not real matches; the contact screen's power readout moved to a vertical side bar; the drag/aim arrow lengthened 20%; a redundant canvas-drawn power meter removed; "The half is over"/"That's the whistle" text removed (the button alone says it); commentary text is white throughout, not gray, and a goal line gets a real green flash instead of a near-invisible tint; the post-match stats screen now uses the same top-aligned frame and scoreboard-plate score display as the rest of the game; the Awards screen's Player of the Month card shows only the winner, not the shortlist.
- Engine bugs fixed: a tackle mid-dribble no longer flashes a stale, unrelated scenario for a moment before cutting to commentary; a ball shot over the bar now keeps flying off-screen instead of stopping dead in mid-air; rebounds off the keeper are now chaseable in the air, not just once they hit the ground (raised the height gates on the poacher poke-in and the loose-ball 50-50).
- **Difficulty investigated and fixed**: the opponent's goals — and your own team's "fell to a teammate" goals — always converted at the same flat rate regardless of team quality; only your own personally-taken shots had any upside beyond it, which is most of why scoring once or twice made a match feel safe to coast. Both flat-rate paths now get an occasional "moment of quality" clinical finish (`convertRate()` in `lib/star/hiddenMatch.ts`).
- **Commentary now tints each line by which team it's about**, using that team's real kit colour and legible ink — a per-line `isOpponent` flag threads from `hiddenMatch.ts` through `matchLog.ts`'s `LogLine` into `MatchCommentary.tsx`.
- **A real, reproducible media bug found and fixed**: a screenshot showed your own club's account posting "BREAKING — Player of the Month, one of our own" the same month the stats account correctly showed someone else winning it. Root cause: the POTM award moment carried no won/lost flag, so the club-congratulation template fired for any shortlist placement, not just an actual win — fixed in `lib/star/media/types.ts`/`detect/career.ts`, with a regression test in `tests/star/media.mts`.
- **Real terrace chants added to the media feed** (`lib/star/media/chants.ts`, user-supplied, growing over several rounds) — club win/result chants (Arsenal's "One-Nil to the Arsenal" specifically at 1-0, Liverpool's win-vs-draw/loss split, etc.) and real-player goal chants (Bellingham, Salah, Havertz's "60 MILLION DOWN THE DRAIN", an eleven-line Sam Smith song, and more). Needed two small additions: exact-match `club`/`score`/`player` gates on `Template` (`templates/index.ts`), and a new `TEAMMATE_GOAL` detector giving a single (not just a 2+) team-mate's goal a named `scorer` fact to match a chant against.
- **Energy pulled from the UI, not the data model — then, later the same day, pulled out entirely.** First pass: training no longer cost or checked it, the dashboard's top energy bar was gone, and the NRG-drinks shop was removed outright (Home page card, buy button, `Shop.tsx`'s whole `"nrg"` tab, `nrgDrinks`/`NRG_DRINKS`) — but `career.energy` itself still existed and still drained/regenerated in the background, and the Life tab's Rest button and relationship minigames still spent it. Told directly to "get rid of all energy for now," the second pass removed it completely: `CareerState.energy` no longer exists as a field at all. Gone with it — `week.ts`'s `rest()`/`startNewWeek()` regen math (Rest still exists, now happiness-only), `dilemmas.ts`'s per-choice energy effects, `selection.ts`'s `MISSED_WEEK.energy` and `hookCheck`'s tired-legs substitution reason (`HookReason` lost `"legs"`), the matchday screen's energy tile/warning, and — the parts that actually did something — `CanvasMatch.tsx`'s `tiredSkills()` (was quietly shaving your power/technique as a match wore on) and `hiddenMatch.ts`'s energy term in the chance-involvement formula (its average weight was folded into the formula's base constant so involvement frequency didn't silently drop). See the "What Needs Improvement" entry above for the real mechanic this is standing in for.

Earlier sessions (19 August missing-images/finishing/cup-draw pass, 7 August
match-engine rebuild, and everything before) moved to `SESSION_LOG.md` to
keep this file readable — see that file for full detail.
